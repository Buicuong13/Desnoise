"""Document vs non-document image classifier (MobileNetV3-Small).

Used to validate that an uploaded image is actually a document page before the
denoise → OCR → LLM pipeline runs. See `notebook4dacb0ffb5.ipynb` for training.
"""
