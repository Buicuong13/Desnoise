id,name,filePath,startLine,endLine,level,content,description
"Section:README.md:L1:Thiết kế hệ thống dự án khử nhiễu + OCR + LLM Correction (Phiên bản 2)","Thiết kế hệ thống dự án khử nhiễu + OCR + LLM Correction (Phiên bản 2)","README.md",1,328,1,"# Thiết kế hệ thống dự án khử nhiễu + OCR + LLM Correction (Phiên bản 2)

## Context

Hệ thống web cho phép người dùng upload ảnh tài liệu (từng trang sách), khử nhiễu bằng model tự xây. **Sau mỗi bước, user chủ động quyết định bước tiếp theo** (pipeline KHÔNG chạy tự động xuyên suốt):

1. Khử nhiễu xong → user có thể **download ảnh denoised về máy và dừng**, hoặc **bấm ""Run OCR""** để trích văn bản.
2. OCR xong → user xem kết quả; nếu thấy OCR ổn → **bỏ qua LLM**, đi thẳng tới export. Nếu chưa ổn → **bấm ""Run LLM Correction""** để LLM đề xuất sửa các từ confidence thấp.
3. LLM trả gợi ý → user review từng đề xuất bằng **Keep / Undo** giống GitHub Copilot.
4. Cuối cùng xuất Word/PDF.

Hỗ trợ **nhiều workspace song song** (mỗi cuốn sách = 1 workspace, mở workspace khác vẫn giữ nguyên state cuốn cũ).

3 role: **Admin / User / Viewer**. Viewer bị giới hạn 10 ảnh, không xuất file; LLM dùng **Qwen free (OpenRouter)**. User trả phí dùng **ChatGPT API**. Admin quản lý toàn bộ.

Tài liệu này chốt: cấu trúc thư mục (bám theo `readme.md` đã có), lược đồ DB, luồng nghiệp vụ, và **3 sơ đồ** (Use Case, Sequence, ERD).

---

## 1. Quyết định công nghệ đã chốt

| Mục | Lựa chọn |
|---|---|
| Backend | Python + FastAPI |
| Database | PostgreSQL |
| Denoise | Model tự xây (OpenCV / Autoencoder) trong `app/ai/denoising/` |
| OCR | Tesseract local (option: PaddleOCR) trong `app/ai/ocr/` |
| LLM Correction | **LangChain chains** trong `app/ai/llm/chains/` — User: OpenAI ChatGPT; Viewer: Qwen free qua OpenRouter |
| Lưu ảnh | Cloudinary (ảnh gốc + ảnh denoised) |
| Auth | JWT (access + refresh), refresh hash lưu DB |
| Async | Celery + Redis (denoise, OCR, LLM correction) |
| Payment | VNPay / MoMo / Stripe (subscription) |
| Xuất file | `python-docx` → `docx2pdf` / LibreOffice headless |

---

## 2. Role & Quyền hạn

### 2.1. Ma trận quyền

| Chức năng | Viewer | User | Admin |
|---|:---:|:---:|:---:|
| Đăng ký / đăng nhập | ✅ | ✅ | ✅ |
| Tạo workspace (document/cuốn sách) | ✅ (≤ 2 workspace) | ✅ (không giới hạn) | ✅ |
| Upload trang (page) vào workspace | ✅ (≤ 10 page **tổng**) | ✅ | ✅ |
| Khử nhiễu ảnh | ✅ | ✅ | ✅ |
| **Download ảnh denoised về máy (dừng ở đây)** | ✅ | ✅ | ✅ |
| Quyết định có chạy OCR hay không | ✅ | ✅ | ✅ |
| Trích OCR (tùy chọn) | ✅ | ✅ | ✅ |
| Quyết định có gọi LLM correction hay không | ✅ | ✅ | ✅ |
| LLM correction (tùy chọn) | ✅ (Qwen free) | ✅ (ChatGPT) | ✅ |
| Review Keep/Undo từng correction | ✅ | ✅ | ✅ |
| Xem text đầy đủ sau phục hồi | ⚠️ **Blur** | ✅ | ✅ |
| Xuất Word (.docx) | ❌ | ✅ | ✅ |
| Xuất PDF | ❌ | ✅ | ✅ |
| Mở nhiều workspace song song & giữ state | ✅ | ✅ | ✅ |
| Xem lịch sử phục hồi của chính mình | ✅ | ✅ | ✅ |
| Nâng cấp tài khoản (payment) | ✅ | — | — |
| Quản lý user (CRUD, ban, role) | ❌ | ❌ | ✅ |
| Xem lịch sử của mọi user | ❌ | ❌ | ✅ |
| Dashboard thống kê + doanh thu | ❌ | ❌ | ✅ |

### 2.2. Giới hạn Viewer

- `images_used ≤ 10` (toàn bộ vòng đời, không reset).
- LLM provider: ép sang Qwen free OpenRouter (chọn ở runtime theo role).
- Backend trả text đầy đủ **kèm flag `blurred=true`** → frontend phủ blur. Endpoint `/exports/*` trả 403.
- Sau khi thanh toán: role → `user`, `images_used` reset 0, LLM provider chuyển sang OpenAI.

---

## 3. Cấu trúc thư mục (bám theo `readme.md`)

Giữ nguyên kiến trúc đã đặt trong `readme.md`. Bổ sung tên file/chức năng cho rõ:

```
backend/
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── config.py         # Pydantic Settings, env
│   │   ├── security.py       # JWT, password hash, RBAC dependency require_roles()
│   │   ├── logging.py
│   │   └── exceptions.py
│   ├── api/
│   │   ├── deps.py           # get_current_user, get_db, get_llm_provider_for_user
│   │   └── v1/
│   │       ├── api.py        # router include
│   │       └── endpoints/
│   │           ├── auth.py            # register/login/refresh/logout
│   │           ├── users.py           # me, update profile
│   │           ├── documents.py       # CRUD workspace, list, ui_state
│   │           ├── pages.py           # POST upload (auto-denoise),
│   │           │                      # GET /pages/{id}/status,
│   │           │                      # GET /pages/{id}/download?type=denoised
│   │           ├── ocr.py             # POST /pages/{id}/ocr  ← USER TRIGGER
│   │           │                      # GET  /pages/{id}/ocr
│   │           ├── restoration.py     # POST /pages/{id}/llm-correction ← USER TRIGGER
│   │           ├── corrections.py     # GET list, POST keep/undo/bulk
│   │           └── admin.py           # admin endpoints
│   ├── schemas/             # Pydantic schemas
│   ├── models/              # SQLAlchemy ORM (xem section 4)
│   │   ├── user.py
│   │   ├── document.py        # = workspace
│   │   ├── page.py            # 1 ảnh upload
│   │   ├── ocr_word.py        # từng từ + confidence + bbox
│   │   ├── correction.py      # đề xuất sửa của LLM + trạng thái 
... [truncated]","h1"
"Section:README.md:L3:Context","Context","README.md",3,19,2,"
## Context

Hệ thống web cho phép người dùng upload ảnh tài liệu (từng trang sách), khử nhiễu bằng model tự xây. **Sau mỗi bước, user chủ động quyết định bước tiếp theo** (pipeline KHÔNG chạy tự động xuyên suốt):

1. Khử nhiễu xong → user có thể **download ảnh denoised về máy và dừng**, hoặc **bấm ""Run OCR""** để trích văn bản.
2. OCR xong → user xem kết quả; nếu thấy OCR ổn → **bỏ qua LLM**, đi thẳng tới export. Nếu chưa ổn → **bấm ""Run LLM Correction""** để LLM đề xuất sửa các từ confidence thấp.
3. LLM trả gợi ý → user review từng đề xuất bằng **Keep / Undo** giống GitHub Copilot.
4. Cuối cùng xuất Word/PDF.

Hỗ trợ **nhiều workspace song song** (mỗi cuốn sách = 1 workspace, mở workspace khác vẫn giữ nguyên state cuốn cũ).

3 role: **Admin / User / Viewer**. Viewer bị giới hạn 10 ảnh, không xuất file; LLM dùng **Qwen free (OpenRouter)**. User trả phí dùng **ChatGPT API**. Admin quản lý toàn bộ.

Tài liệu này chốt: cấu trúc thư mục (bám theo `readme.md` đã có), lược đồ DB, luồng nghiệp vụ, và **3 sơ đồ** (Use Case, Sequence, ERD).

---

## 1. Quyết định công nghệ đã chốt

| Mục | Lựa chọn |
","h2"
"Section:README.md:L20:1. Quyết định công nghệ đã chốt","1. Quyết định công nghệ đã chốt","README.md",20,36,2,"
## 1. Quyết định công nghệ đã chốt

| Mục | Lựa chọn |
|---|---|
| Backend | Python + FastAPI |
| Database | PostgreSQL |
| Denoise | Model tự xây (OpenCV / Autoencoder) trong `app/ai/denoising/` |
| OCR | Tesseract local (option: PaddleOCR) trong `app/ai/ocr/` |
| LLM Correction | **LangChain chains** trong `app/ai/llm/chains/` — User: OpenAI ChatGPT; Viewer: Qwen free qua OpenRouter |
| Lưu ảnh | Cloudinary (ảnh gốc + ảnh denoised) |
| Auth | JWT (access + refresh), refresh hash lưu DB |
| Async | Celery + Redis (denoise, OCR, LLM correction) |
| Payment | VNPay / MoMo / Stripe (subscription) |
| Xuất file | `python-docx` → `docx2pdf` / LibreOffice headless |

---

## 2. Role & Quyền hạn

### 2.1. Ma trận quyền
","h2"
"Section:README.md:L37:2. Role & Quyền hạn","2. Role & Quyền hạn","README.md",37,71,2,"
## 2. Role & Quyền hạn

### 2.1. Ma trận quyền

| Chức năng | Viewer | User | Admin |
|---|:---:|:---:|:---:|
| Đăng ký / đăng nhập | ✅ | ✅ | ✅ |
| Tạo workspace (document/cuốn sách) | ✅ (≤ 2 workspace) | ✅ (không giới hạn) | ✅ |
| Upload trang (page) vào workspace | ✅ (≤ 10 page **tổng**) | ✅ | ✅ |
| Khử nhiễu ảnh | ✅ | ✅ | ✅ |
| **Download ảnh denoised về máy (dừng ở đây)** | ✅ | ✅ | ✅ |
| Quyết định có chạy OCR hay không | ✅ | ✅ | ✅ |
| Trích OCR (tùy chọn) | ✅ | ✅ | ✅ |
| Quyết định có gọi LLM correction hay không | ✅ | ✅ | ✅ |
| LLM correction (tùy chọn) | ✅ (Qwen free) | ✅ (ChatGPT) | ✅ |
| Review Keep/Undo từng correction | ✅ | ✅ | ✅ |
| Xem text đầy đủ sau phục hồi | ⚠️ **Blur** | ✅ | ✅ |
| Xuất Word (.docx) | ❌ | ✅ | ✅ |
| Xuất PDF | ❌ | ✅ | ✅ |
| Mở nhiều workspace song song & giữ state | ✅ | ✅ | ✅ |
| Xem lịch sử phục hồi của chính mình | ✅ | ✅ | ✅ |
| Nâng cấp tài khoản (payment) | ✅ | — | — |
| Quản lý user (CRUD, ban, role) | ❌ | ❌ | ✅ |
| Xem lịch sử của mọi user | ❌ | ❌ | ✅ |
| Dashboard thống kê + doanh thu | ❌ | ❌ | ✅ |

### 2.2. Giới hạn Viewer

- `images_used ≤ 10` (toàn bộ vòng đời, không reset).
- LLM provider: ép sang Qwen free OpenRouter (chọn ở runtime theo role).
- Backend trả text đầy đủ **kèm flag `blurred=true`** → frontend phủ blur. Endpoint `/exports/*` trả 403.
- Sau khi thanh toán: role → `user`, `images_used` reset 0, LLM provider chuyển sang OpenAI.

---

## 3. Cấu trúc thư mục (bám theo `readme.md`)

Giữ nguyên kiến trúc đã đặt trong `readme.md`. Bổ sung tên file/chức năng cho rõ:
","h2"
"Section:README.md:L39:2.1. Ma trận quyền","2.1. Ma trận quyền","README.md",39,62,3,"
### 2.1. Ma trận quyền

| Chức năng | Viewer | User | Admin |
|---|:---:|:---:|:---:|
| Đăng ký / đăng nhập | ✅ | ✅ | ✅ |
| Tạo workspace (document/cuốn sách) | ✅ (≤ 2 workspace) | ✅ (không giới hạn) | ✅ |
| Upload trang (page) vào workspace | ✅ (≤ 10 page **tổng**) | ✅ | ✅ |
| Khử nhiễu ảnh | ✅ | ✅ | ✅ |
| **Download ảnh denoised về máy (dừng ở đây)** | ✅ | ✅ | ✅ |
| Quyết định có chạy OCR hay không | ✅ | ✅ | ✅ |
| Trích OCR (tùy chọn) | ✅ | ✅ | ✅ |
| Quyết định có gọi LLM correction hay không | ✅ | ✅ | ✅ |
| LLM correction (tùy chọn) | ✅ (Qwen free) | ✅ (ChatGPT) | ✅ |
| Review Keep/Undo từng correction | ✅ | ✅ | ✅ |
| Xem text đầy đủ sau phục hồi | ⚠️ **Blur** | ✅ | ✅ |
| Xuất Word (.docx) | ❌ | ✅ | ✅ |
| Xuất PDF | ❌ | ✅ | ✅ |
| Mở nhiều workspace song song & giữ state | ✅ | ✅ | ✅ |
| Xem lịch sử phục hồi của chính mình | ✅ | ✅ | ✅ |
| Nâng cấp tài khoản (payment) | ✅ | — | — |
| Quản lý user (CRUD, ban, role) | ❌ | ❌ | ✅ |
| Xem lịch sử của mọi user | ❌ | ❌ | ✅ |
| Dashboard thống kê + doanh thu | ❌ | ❌ | ✅ |

### 2.2. Giới hạn Viewer

- `images_used ≤ 10` (toàn bộ vòng đời, không reset).
","h3"
"Section:README.md:L63:2.2. Giới hạn Viewer","2.2. Giới hạn Viewer","README.md",63,71,3,"
### 2.2. Giới hạn Viewer

- `images_used ≤ 10` (toàn bộ vòng đời, không reset).
- LLM provider: ép sang Qwen free OpenRouter (chọn ở runtime theo role).
- Backend trả text đầy đủ **kèm flag `blurred=true`** → frontend phủ blur. Endpoint `/exports/*` trả 403.
- Sau khi thanh toán: role → `user`, `images_used` reset 0, LLM provider chuyển sang OpenAI.

---

## 3. Cấu trúc thư mục (bám theo `readme.md`)

Giữ nguyên kiến trúc đã đặt trong `readme.md`. Bổ sung tên file/chức năng cho rõ:
","h3"
"Section:README.md:L72:3. Cấu trúc thư mục (bám theo `readme.md`)","3. Cấu trúc thư mục (bám theo `readme.md`)","README.md",72,155,2,"
## 3. Cấu trúc thư mục (bám theo `readme.md`)

Giữ nguyên kiến trúc đã đặt trong `readme.md`. Bổ sung tên file/chức năng cho rõ:

```
backend/
├── app/
│   ├── main.py
│   ├── core/
│   │   ├── config.py         # Pydantic Settings, env
│   │   ├── security.py       # JWT, password hash, RBAC dependency require_roles()
│   │   ├── logging.py
│   │   └── exceptions.py
│   ├── api/
│   │   ├── deps.py           # get_current_user, get_db, get_llm_provider_for_user
│   │   └── v1/
│   │       ├── api.py        # router include
│   │       └── endpoints/
│   │           ├── auth.py            # register/login/refresh/logout
│   │           ├── users.py           # me, update profile
│   │           ├── documents.py       # CRUD workspace, list, ui_state
│   │           ├── pages.py           # POST upload (auto-denoise),
│   │           │                      # GET /pages/{id}/status,
│   │           │                      # GET /pages/{id}/download?type=denoised
│   │           ├── ocr.py             # POST /pages/{id}/ocr  ← USER TRIGGER
│   │           │                      # GET  /pages/{id}/ocr
│   │           ├── restoration.py     # POST /pages/{id}/llm-correction ← USER TRIGGER
│   │           ├── corrections.py     # GET list, POST keep/undo/bulk
│   │           └── admin.py           # admin endpoints
│   ├── schemas/             # Pydantic schemas
│   ├── models/              # SQLAlchemy ORM (xem section 4)
│   │   ├── user.py
│   │   ├── document.py        # = workspace
│   │   ├── page.py            # 1 ảnh upload
│   │   ├── ocr_word.py        # từng từ + confidence + bbox
│   │   ├── correction.py      # đề xuất sửa của LLM + trạng thái keep/undo
│   │   └── feedback.py        # ghi chú user
│   ├── repositories/        # DB access layer
│   ├── services/
│   │   ├── document_service.py            # quản lý workspace, switch, snapshot state
│   │   ├── image_restoration_service.py   # gọi denoise
│   │   ├── ocr_service.py
│   │   ├── suspicious_detector_service.py # lọc word theo confidence threshold
│   │   ├── llm_correction_service.py      # gọi LangChain chain, chọn provider theo role
│   │   ├── correction_review_service.py   # apply keep/undo, build final text
│   │   └── admin_service.py
│   ├── ai/
│   │   ├── denoising/        # model_loader, inference, transforms
│   │   ├── ocr/              # base, tesseract_engine, paddleocr_engine
│   │   └── llm/
│   │       ├── chains/
│   │       │   └── ocr_correction_chain.py   # LangChain LCEL chain
│   │       ├── prompts/
│   │       │   └── ocr_correction_prompt.py  # prompt template (Vietnamese OCR fix)
│   │       ├── output_parsers.py             # Pydantic parser cho list[Correction]
│   │       └── schemas.py                    # CorrectionSuggestion pydantic
│   ├── workers/
│   │   ├── celery_app.py
│   │   └── tasks.py          # denoise_task, ocr_task, llm_correction_task
│   ├── database/
│   ├── utils/
│   └── storage/
├── alembic/
├── tests/
├── requirements.txt
├── pyproject.toml
└── .env
```

### File quan trọng phải tạo

- [backend/app/models/document.py](backend/app/models/document.py) — model `Document` = workspace
- [backend/app/models/page.py](backend/app/models/page.py) — 1 ảnh thuộc 1 document
- [backend/app/models/ocr_word.py](backend/app/models/ocr_word.py) — từng word + confidence + bbox
- [backend/app/models/correction.py](backend/app/models/correction.py) — đề xuất LLM + state `pending/kept/undone`
- [backend/app/ai/llm/chains/ocr_correction_chain.py](backend/app/ai/llm/chains/ocr_correction_chain.py) — LangChain chain
- [backend/app/ai/llm/prompts/ocr_correction_prompt.py](backend/app/ai/llm/prompts/ocr_correction_prompt.py)
- [backend/app/services/suspicious_detector_service.py](backend/app/services/suspicious_detector_service.py)
- [backend/app/services/correction_review_service.py](backend/app/services/correction_review_service.py)
- [backend/app/services/document_service.py](backend/app/services/document_service.py) — snapshot state workspace
- [backend/app/api/deps.py](backend/app/api/deps.py) — dependency `get_llm_provider_for_user()` chọn provider theo role

---

## 4. Database Schema (PostgreSQL)

12 bảng. Nhóm theo: Account / Workspace / AI Pipeline / Subscription / Audit.
","h2"
"Section:README.md:L141:File quan trọng phải tạo","File quan trọng phải tạo","README.md",141,155,3,"
### File quan trọng phải tạo

- [backend/app/models/document.py](backend/app/models/document.py) — model `Document` = workspace
- [backend/app/models/page.py](backend/app/models/page.py) — 1 ảnh thuộc 1 document
- [backend/app/models/ocr_word.py](backend/app/models/ocr_word.py) — từng word + confidence + bbox
- [backend/app/models/correction.py](backend/app/models/correction.py) — đề xuất LLM + state `pending/kept/undone`
- [backend/app/ai/llm/chains/ocr_correction_chain.py](backend/app/ai/llm/chains/ocr_correction_chain.py) — LangChain chain
- [backend/app/ai/llm/prompts/ocr_correction_prompt.py](backend/app/ai/llm/prompts/ocr_correction_prompt.py)
- [backend/app/services/suspicious_detector_service.py](backend/app/services/suspicious_detector_service.py)
- [backend/app/services/correction_review_service.py](backend/app/services/correction_review_service.py)
- [backend/app/services/document_service.py](backend/app/services/document_service.py) — snapshot state workspace
- [backend/app/api/deps.py](backend/app/api/deps.py) — dependency `get_llm_provider_for_user()` chọn provider theo role

---

## 4. Database Schema (PostgreSQL)

12 bảng. Nhóm theo: Account / Workspace / AI Pipeline / Subscription / Audit.
","h3"
"Section:README.md:L156:4. Database Schema (PostgreSQL)","4. Database Schema (PostgreSQL)","README.md",156,310,2,"
## 4. Database Schema (PostgreSQL)

12 bảng. Nhóm theo: Account / Workspace / AI Pipeline / Subscription / Audit.

### 4.1. `users`
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| email | VARCHAR(255) UNIQUE | |
| password_hash | VARCHAR(255) | argon2id |
| full_name | VARCHAR(150) | |
| role | ENUM('admin','user','viewer') DEFAULT 'viewer' | |
| status | ENUM('active','banned','pending') DEFAULT 'active' | |
| images_used | INT DEFAULT 0 | quota Viewer |
| active_workspace_id | UUID NULL FK → documents(id) | workspace user đang mở gần nhất |
| created_at, updated_at, last_login_at | TIMESTAMPTZ | |

### 4.2. `refresh_tokens`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user_id | UUID FK → users(id) CASCADE |
| token_hash | VARCHAR(255) |
| expires_at, revoked_at | TIMESTAMPTZ |
| user_agent, ip | VARCHAR |

### 4.3. `documents` (= **Workspace** / cuốn sách)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| user_id | UUID FK → users(id) | |
| title | VARCHAR(255) | ""Truyện Kiều - bản A"" |
| description | TEXT NULL | |
| status | ENUM('draft','processing','ready','archived') DEFAULT 'draft' | |
| total_pages | INT DEFAULT 0 | |
| ui_state | JSONB | **lưu state UI**: trang đang xem, vị trí cuộn, filter — cho phép quay lại workspace mà không mất ngữ cảnh |
| last_opened_at | TIMESTAMPTZ | |
| created_at, updated_at | TIMESTAMPTZ | |

Index: `(user_id, last_opened_at DESC)`. User có nhiều documents, mỗi document độc lập → switch workspace chỉ cần đổi `active_workspace_id` ở client; backend không có session ""global"" — mọi state nằm trên row `documents`.

### 4.4. `pages` (1 page = 1 ảnh trong workspace)
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) CASCADE |
| page_number | INT — số thứ tự trong workspace |
| cloudinary_public_id | VARCHAR(255) |
| original_url | TEXT |
| denoised_url | TEXT NULL |
| file_size_kb, width, height | INT |
| status | ENUM('uploaded','denoising','denoised','ocr_running','ocr_done','llm_running','llm_done','failed') — KHÔNG có 'completed' tự động; user quyết định dừng ở đâu |
| processing_error | TEXT NULL |
| created_at, completed_at | TIMESTAMPTZ |

UNIQUE `(document_id, page_number)`.

### 4.5. `ocr_words` (kết quả OCR per word)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | BIGSERIAL PK | |
| page_id | UUID FK → pages(id) CASCADE | |
| word_index | INT | thứ tự trong page |
| text | TEXT | từ OCR ra |
| confidence | NUMERIC(5,2) | 0..100 từ Tesseract |
| bbox | JSONB | `{x,y,w,h}` |
| line_number | INT | nhóm theo dòng |
| is_suspicious | BOOLEAN DEFAULT false | true nếu confidence < threshold (vd. 70) |
| created_at | TIMESTAMPTZ | |

Index: `(page_id, word_index)`, partial index `WHERE is_suspicious=true`.

### 4.6. `corrections` (đề xuất sửa của LLM — cốt lõi cơ chế Keep/Undo)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| page_id | UUID FK → pages(id) CASCADE | |
| ocr_word_id | BIGINT FK → ocr_words(id) NULL | NULL nếu là đoạn nhiều từ |
| word_indices | INT[] | các index trong ocr_words mà correction này thay thế |
| original_text | TEXT | text gốc từ OCR |
| suggested_text | TEXT | text LLM đề xuất |
| reason | TEXT NULL | LLM giải thích vì sao sửa |
| llm_provider | ENUM('openai','openrouter_qwen') | |
| llm_model | VARCHAR(100) | ""gpt-4o-mini"", ""qwen/qwen-2.5-7b-instruct:free"" |
| confidence_score | NUMERIC(5,2) NULL | độ tin cậy LLM tự đánh giá |
| status | ENUM('pending','kept','undone') DEFAULT 'pending' | **giống Copilot keep/undo** |
| reviewed_at | TIMESTAMPTZ NULL | |
| reviewed_by | UUID FK → users(id) NULL | |
| created_at | TIMESTAMPTZ | |

Index: `(page_id, status)`. Khi user bấm ""Keep"" → `status='kept'`; ""Undo"" → `status='undone'`. **Bản text cuối** dựng từ: với mỗi word, nếu có correction `kept` thì dùng `suggested_text`, ngược lại dùng `text` gốc.

### 4.7. `feedback` (ghi chú phục hồi của user)
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) CASCADE |
| page_id | UUID FK → pages(id) NULL |
| user_id | UUID FK → users(id) |
| note | TEXT |
| created_at, updated_at | TIMESTAMPTZ |

### 4.8. `exports`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) |
| user_id | UUID FK → users(id) |
| format | ENUM('docx','pdf') |
| file_url | TEXT |
| file_size_kb | INT |
| created_at | TIMESTAMPTZ |

### 4.9. `subscription_plans`
| Cột | Kiểu |
|---|---|
| id | SERIAL PK |
| code | VARCHAR(30) UNIQUE — 'monthly','yearly' |
| name, price_vnd, duration_days, features (JSONB), is_active | … |

### 4.10. `user_subscriptions`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user_id | UUID FK → users(id) |
| plan_id | INT FK → subscription_plans(id) |
| status | ENUM('active','expired','cancelled') |
| starts_at, ends_at, created_at | TIMESTAMPTZ |

### 4.11. `payments`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user
... [truncated]","h2"
"Section:README.md:L160:4.1. `users`","4.1. `users`","README.md",160,172,3,"
### 4.1. `users`
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| email | VARCHAR(255) UNIQUE | |
| password_hash | VARCHAR(255) | argon2id |
| full_name | VARCHAR(150) | |
| role | ENUM('admin','user','viewer') DEFAULT 'viewer' | |
| status | ENUM('active','banned','pending') DEFAULT 'active' | |
| images_used | INT DEFAULT 0 | quota Viewer |
| active_workspace_id | UUID NULL FK → documents(id) | workspace user đang mở gần nhất |
| created_at, updated_at, last_login_at | TIMESTAMPTZ | |

### 4.2. `refresh_tokens`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L173:4.2. `refresh_tokens`","4.2. `refresh_tokens`","README.md",173,181,3,"
### 4.2. `refresh_tokens`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user_id | UUID FK → users(id) CASCADE |
| token_hash | VARCHAR(255) |
| expires_at, revoked_at | TIMESTAMPTZ |
| user_agent, ip | VARCHAR |

### 4.3. `documents` (= **Workspace** / cuốn sách)
| Cột | Kiểu | Ghi chú |
|---|---|---|
","h3"
"Section:README.md:L182:4.3. `documents` (= **Workspace** / cuốn sách)","4.3. `documents` (= **Workspace** / cuốn sách)","README.md",182,196,3,"
### 4.3. `documents` (= **Workspace** / cuốn sách)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| user_id | UUID FK → users(id) | |
| title | VARCHAR(255) | ""Truyện Kiều - bản A"" |
| description | TEXT NULL | |
| status | ENUM('draft','processing','ready','archived') DEFAULT 'draft' | |
| total_pages | INT DEFAULT 0 | |
| ui_state | JSONB | **lưu state UI**: trang đang xem, vị trí cuộn, filter — cho phép quay lại workspace mà không mất ngữ cảnh |
| last_opened_at | TIMESTAMPTZ | |
| created_at, updated_at | TIMESTAMPTZ | |

Index: `(user_id, last_opened_at DESC)`. User có nhiều documents, mỗi document độc lập → switch workspace chỉ cần đổi `active_workspace_id` ở client; backend không có session ""global"" — mọi state nằm trên row `documents`.

### 4.4. `pages` (1 page = 1 ảnh trong workspace)
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L197:4.4. `pages` (1 page = 1 ảnh trong workspace)","4.4. `pages` (1 page = 1 ảnh trong workspace)","README.md",197,212,3,"
### 4.4. `pages` (1 page = 1 ảnh trong workspace)
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) CASCADE |
| page_number | INT — số thứ tự trong workspace |
| cloudinary_public_id | VARCHAR(255) |
| original_url | TEXT |
| denoised_url | TEXT NULL |
| file_size_kb, width, height | INT |
| status | ENUM('uploaded','denoising','denoised','ocr_running','ocr_done','llm_running','llm_done','failed') — KHÔNG có 'completed' tự động; user quyết định dừng ở đâu |
| processing_error | TEXT NULL |
| created_at, completed_at | TIMESTAMPTZ |

UNIQUE `(document_id, page_number)`.

### 4.5. `ocr_words` (kết quả OCR per word)
| Cột | Kiểu | Ghi chú |
|---|---|---|
","h3"
"Section:README.md:L213:4.5. `ocr_words` (kết quả OCR per word)","4.5. `ocr_words` (kết quả OCR per word)","README.md",213,227,3,"
### 4.5. `ocr_words` (kết quả OCR per word)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | BIGSERIAL PK | |
| page_id | UUID FK → pages(id) CASCADE | |
| word_index | INT | thứ tự trong page |
| text | TEXT | từ OCR ra |
| confidence | NUMERIC(5,2) | 0..100 từ Tesseract |
| bbox | JSONB | `{x,y,w,h}` |
| line_number | INT | nhóm theo dòng |
| is_suspicious | BOOLEAN DEFAULT false | true nếu confidence < threshold (vd. 70) |
| created_at | TIMESTAMPTZ | |

Index: `(page_id, word_index)`, partial index `WHERE is_suspicious=true`.

### 4.6. `corrections` (đề xuất sửa của LLM — cốt lõi cơ chế Keep/Undo)
| Cột | Kiểu | Ghi chú |
|---|---|---|
","h3"
"Section:README.md:L228:4.6. `corrections` (đề xuất sửa của LLM — cốt lõi cơ chế Keep/Undo)","4.6. `corrections` (đề xuất sửa của LLM — cốt lõi cơ chế Keep/Undo)","README.md",228,247,3,"
### 4.6. `corrections` (đề xuất sửa của LLM — cốt lõi cơ chế Keep/Undo)
| Cột | Kiểu | Ghi chú |
|---|---|---|
| id | UUID PK | |
| page_id | UUID FK → pages(id) CASCADE | |
| ocr_word_id | BIGINT FK → ocr_words(id) NULL | NULL nếu là đoạn nhiều từ |
| word_indices | INT[] | các index trong ocr_words mà correction này thay thế |
| original_text | TEXT | text gốc từ OCR |
| suggested_text | TEXT | text LLM đề xuất |
| reason | TEXT NULL | LLM giải thích vì sao sửa |
| llm_provider | ENUM('openai','openrouter_qwen') | |
| llm_model | VARCHAR(100) | ""gpt-4o-mini"", ""qwen/qwen-2.5-7b-instruct:free"" |
| confidence_score | NUMERIC(5,2) NULL | độ tin cậy LLM tự đánh giá |
| status | ENUM('pending','kept','undone') DEFAULT 'pending' | **giống Copilot keep/undo** |
| reviewed_at | TIMESTAMPTZ NULL | |
| reviewed_by | UUID FK → users(id) NULL | |
| created_at | TIMESTAMPTZ | |

Index: `(page_id, status)`. Khi user bấm ""Keep"" → `status='kept'`; ""Undo"" → `status='undone'`. **Bản text cuối** dựng từ: với mỗi word, nếu có correction `kept` thì dùng `suggested_text`, ngược lại dùng `text` gốc.

### 4.7. `feedback` (ghi chú phục hồi của user)
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L248:4.7. `feedback` (ghi chú phục hồi của user)","4.7. `feedback` (ghi chú phục hồi của user)","README.md",248,257,3,"
### 4.7. `feedback` (ghi chú phục hồi của user)
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) CASCADE |
| page_id | UUID FK → pages(id) NULL |
| user_id | UUID FK → users(id) |
| note | TEXT |
| created_at, updated_at | TIMESTAMPTZ |

### 4.8. `exports`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L258:4.8. `exports`","4.8. `exports`","README.md",258,268,3,"
### 4.8. `exports`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| document_id | UUID FK → documents(id) |
| user_id | UUID FK → users(id) |
| format | ENUM('docx','pdf') |
| file_url | TEXT |
| file_size_kb | INT |
| created_at | TIMESTAMPTZ |

### 4.9. `subscription_plans`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L269:4.9. `subscription_plans`","4.9. `subscription_plans`","README.md",269,275,3,"
### 4.9. `subscription_plans`
| Cột | Kiểu |
|---|---|
| id | SERIAL PK |
| code | VARCHAR(30) UNIQUE — 'monthly','yearly' |
| name, price_vnd, duration_days, features (JSONB), is_active | … |

### 4.10. `user_subscriptions`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L276:4.10. `user_subscriptions`","4.10. `user_subscriptions`","README.md",276,284,3,"
### 4.10. `user_subscriptions`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user_id | UUID FK → users(id) |
| plan_id | INT FK → subscription_plans(id) |
| status | ENUM('active','expired','cancelled') |
| starts_at, ends_at, created_at | TIMESTAMPTZ |

### 4.11. `payments`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L285:4.11. `payments`","4.11. `payments`","README.md",285,297,3,"
### 4.11. `payments`
| Cột | Kiểu |
|---|---|
| id | UUID PK |
| user_id | UUID FK |
| subscription_id | UUID FK NULL |
| gateway | ENUM('vnpay','momo','stripe') |
| gateway_txn_id | VARCHAR(100) UNIQUE |
| amount_vnd | INT |
| status | ENUM('pending','success','failed','refunded') |
| raw_payload | JSONB |
| created_at, paid_at | TIMESTAMPTZ |

### 4.12. `audit_logs`
| Cột | Kiểu |
|---|---|
","h3"
"Section:README.md:L298:4.12. `audit_logs`","4.12. `audit_logs`","README.md",298,310,3,"
### 4.12. `audit_logs`
| Cột | Kiểu |
|---|---|
| id | BIGSERIAL PK |
| actor_id | UUID FK → users(id) |
| action | VARCHAR(50) |
| target_type, target_id | VARCHAR |
| metadata | JSONB |
| ip | INET |
| created_at | TIMESTAMPTZ |

---

## 5. Luồng LLM Correction (LangChain) — chi tiết

### 5.1. Suspicious detector
","h3"
"Section:README.md:L311:5. Luồng LLM Correction (LangChain) — chi tiết","5. Luồng LLM Correction (LangChain) — chi tiết","README.md",311,328,2,"
## 5. Luồng LLM Correction (LangChain) — chi tiết

### 5.1. Suspicious detector
File: `services/suspicious_detector_service.py`

```
INPUT:  page_id
LOGIC:
  - Đọc ocr_words của page
  - Đánh dấu is_suspicious = True nếu confidence < THRESHOLD (mặc định 70%)
  - Gom các từ suspicious liền kề thành cụm (chunk) để có context tốt hơn
OUTPUT: List[SuspiciousChunk{word_indices, surrounding_context, original_text}]
```

### 5.2. LLM Correction Chain (LangChain LCEL)
File: `ai/llm/chains/ocr_correction_chain.py`

```python
# pseudo
prompt = ChatPromptTemplate.from_messages([
    (""system"", OCR_CORRECTION_SYSTEM_PROMPT),
","h2"
"Section:README.md:L313:5.1. Suspicious detector","5.1. Suspicious detector","README.md",313,324,3,"
### 5.1. Suspicious detector
File: `services/suspicious_detector_service.py`

```
INPUT:  page_id
LOGIC:
  - Đọc ocr_words của page
  - Đánh dấu is_suspicious = True nếu confidence < THRESHOLD (mặc định 70%)
  - Gom các từ suspicious liền kề thành cụm (chunk) để có context tốt hơn
OUTPUT: List[SuspiciousChunk{word_indices, surrounding_context, original_text}]
```

### 5.2. LLM Correction Chain (LangChain LCEL)
File: `ai/llm/chains/ocr_correction_chain.py`

","h3"
"Section:README.md:L325:5.2. LLM Correction Chain (LangChain LCEL)","5.2. LLM Correction Chain (LangChain LCEL)","README.md",325,328,3,"
### 5.2. LLM Correction Chain (LangChain LCEL)
File: `ai/llm/chains/ocr_correction_chain.py`

```python
# pseudo
prompt = ChatPromptTemplate.from_messages([
    (""system"", OCR_CORRECTION_SYSTEM_PROMPT),
","h3"
"Section:README.md:L329:pseudo","pseudo","README.md",329,783,1,"```python
# pseudo
prompt = ChatPromptTemplate.from_messages([
    (""system"", OCR_CORRECTION_SYSTEM_PROMPT),
    (""human"", ""Context: {context}\nOCR có vẻ sai: {original}\n""
              ""Trả về JSON list [{original, suggested, reason}]."")
])
parser = PydanticOutputParser(pydantic_object=CorrectionList)

def build_chain(role: str):
    llm = (
        ChatOpenAI(model=""gpt-4o-mini"")           # role = user/admin
        if role in (""user"", ""admin"")
        else ChatOpenAI(                           # role = viewer
            model=""qwen/qwen-2.5-7b-instruct:free"",
            base_url=""https://openrouter.ai/api/v1"",
            api_key=settings.OPENROUTER_KEY,
        )
    )
    return prompt | llm | parser
```

Service `llm_correction_service.py`:
1. Lấy danh sách chunks từ suspicious_detector.
2. Build chain theo role qua `get_llm_provider_for_user(user)`.
3. Batch invoke (LangChain `chain.batch([...])`) — mỗi chunk 1 input.
4. Với mỗi suggestion → insert row `corrections` với `status='pending'`.

### 5.3. Review Keep/Undo (giống Copilot)
Endpoint `corrections.py`:
- `GET /documents/{doc_id}/pages/{page_id}/corrections?status=pending` → frontend render dạng diff inline.
- `POST /corrections/{id}/keep` → set `status='kept'`, `reviewed_at`, `reviewed_by`.
- `POST /corrections/{id}/undo` → set `status='undone'`.
- `POST /corrections/bulk` body `{accept_ids:[], reject_ids:[]}` — apply nhiều lần một lúc.

Build text cuối (service `correction_review_service.py`):
```
SELECT ocr_words của page, JOIN với corrections WHERE status='kept'
FOR each word_index:
    if kept_correction exists: emit suggested_text
    else: emit original text
```
Kết quả: text ""phục hồi"" để xuất Word/PDF.

---

## 6. Cơ chế Multi-Workspace (nhiều cuốn sách song song)

Yêu cầu: User mở nhiều workspace, switch qua lại không mất state.

**Backend không stateful**. Toàn bộ state nằm trên row `documents`:
- `documents.ui_state` JSONB lưu: page đang xem, vị trí cuộn, filter ""chỉ hiện corrections pending"", scale zoom ảnh, side panel mở/đóng…
- `users.active_workspace_id` lưu workspace cuối user mở (để frontend default về khi reload).
- `documents.last_opened_at` cập nhật mỗi lần GET workspace → list workspace sắp xếp theo recency.

**Frontend** dùng tab UI:
- Mỗi tab = 1 workspace (document). Tab giữ state cục bộ trong React state + đồng bộ về `documents.ui_state` mỗi N giây (debounce).
- Đóng tab không xóa workspace, chỉ đóng UI.
- Switch tab = GET `/documents/{id}/state` → render lại.

**API workspace**:
- `POST /documents` — tạo workspace mới.
- `GET /documents` — list workspace của user, sắp xếp `last_opened_at DESC`.
- `GET /documents/{id}` — full state (pages + corrections + ui_state).
- `PATCH /documents/{id}/ui-state` — debounced save từ frontend.
- `DELETE /documents/{id}` — archive (soft-delete bằng `status='archived'`).

---

## 7. Luồng nghiệp vụ end-to-end (pipeline có điểm dừng)

**Nguyên tắc**: Mỗi bước AI (denoise → OCR → LLM) là một **API riêng**, chạy độc lập, KHÔNG tự trigger bước kế tiếp. User quyết định.

### Bước 1 — Workspace
1. User login → GET `/documents` → render list workspace.
2. Chọn workspace cũ HOẶC `POST /documents` tạo mới.

### Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)
3. `POST /documents/{id}/pages/upload` → upload Cloudinary → ghi `pages(status='uploaded')` → enqueue `denoise_task`.
4. Worker: denoise → upload ảnh denoised → `pages.status='denoised'` (KHÔNG tự chạy OCR).
5. Frontend poll/WebSocket → biết denoise xong → hiển thị ảnh trước/sau.

### Bước 3 — User QUYẾT ĐỊNH:
   - **Option A — Download và dừng**: `GET /pages/{id}/download?type=denoised` → trả file ảnh denoised → user lưu về máy → KẾT THÚC luồng cho page này. `pages.status` vẫn là `denoised`, có thể quay lại chạy OCR sau.
   - **Option B — Chạy OCR**: `POST /pages/{id}/ocr` → enqueue `ocr_task`.

### Bước 4 — OCR (chỉ chạy khi user yêu cầu)
6. Worker chạy Tesseract → ghi `ocr_words` (text, confidence, bbox, is_suspicious) → `pages.status='ocr_done'`.
7. Frontend GET `/pages/{id}/ocr` → hiển thị text + highlight các từ có `is_suspicious=true` (confidence thấp).

### Bước 5 — User QUYẾT ĐỊNH:
   - **Option A — OCR đã ổn, bỏ qua LLM**: user có thể bấm Export ngay (xem bước 7).
   - **Option B — Gọi LLM correction**: `POST /pages/{id}/llm-correction` → backend chọn provider theo role (ChatGPT/Qwen free) → enqueue `llm_correction_task`.

### Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)
8. Worker: SuspiciousDetector → build chunks → LangChain `chain.batch()` → ghi `corrections(status='pending')` → `pages.status='llm_done'`.
9. Frontend GET `/pages/{id}/corrections?status=pending` → render diff inline.
10. User review từng đề xuất → `POST /corrections/{id}/keep` hoặc `/undo` (hoặc bulk).

### Bước 7 — Export (tùy chọn, chỉ User/Admin)
11. `POST /documents/{id}/exports {format:'docx'|'pdf'}` → backend build final text từ `ocr_words` + corr
... [truncated]","h1"
"Section:README.md:L356:5.3. Review Keep/Undo (giống Copilot)","5.3. Review Keep/Undo (giống Copilot)","README.md",356,373,3,"
### 5.3. Review Keep/Undo (giống Copilot)
Endpoint `corrections.py`:
- `GET /documents/{doc_id}/pages/{page_id}/corrections?status=pending` → frontend render dạng diff inline.
- `POST /corrections/{id}/keep` → set `status='kept'`, `reviewed_at`, `reviewed_by`.
- `POST /corrections/{id}/undo` → set `status='undone'`.
- `POST /corrections/bulk` body `{accept_ids:[], reject_ids:[]}` — apply nhiều lần một lúc.

Build text cuối (service `correction_review_service.py`):
```
SELECT ocr_words của page, JOIN với corrections WHERE status='kept'
FOR each word_index:
    if kept_correction exists: emit suggested_text
    else: emit original text
```
Kết quả: text ""phục hồi"" để xuất Word/PDF.

---

## 6. Cơ chế Multi-Workspace (nhiều cuốn sách song song)

Yêu cầu: User mở nhiều workspace, switch qua lại không mất state.
","h3"
"Section:README.md:L374:6. Cơ chế Multi-Workspace (nhiều cuốn sách song song)","6. Cơ chế Multi-Workspace (nhiều cuốn sách song song)","README.md",374,396,2,"
## 6. Cơ chế Multi-Workspace (nhiều cuốn sách song song)

Yêu cầu: User mở nhiều workspace, switch qua lại không mất state.

**Backend không stateful**. Toàn bộ state nằm trên row `documents`:
- `documents.ui_state` JSONB lưu: page đang xem, vị trí cuộn, filter ""chỉ hiện corrections pending"", scale zoom ảnh, side panel mở/đóng…
- `users.active_workspace_id` lưu workspace cuối user mở (để frontend default về khi reload).
- `documents.last_opened_at` cập nhật mỗi lần GET workspace → list workspace sắp xếp theo recency.

**Frontend** dùng tab UI:
- Mỗi tab = 1 workspace (document). Tab giữ state cục bộ trong React state + đồng bộ về `documents.ui_state` mỗi N giây (debounce).
- Đóng tab không xóa workspace, chỉ đóng UI.
- Switch tab = GET `/documents/{id}/state` → render lại.

**API workspace**:
- `POST /documents` — tạo workspace mới.
- `GET /documents` — list workspace của user, sắp xếp `last_opened_at DESC`.
- `GET /documents/{id}` — full state (pages + corrections + ui_state).
- `PATCH /documents/{id}/ui-state` — debounced save từ frontend.
- `DELETE /documents/{id}` — archive (soft-delete bằng `status='archived'`).

---

## 7. Luồng nghiệp vụ end-to-end (pipeline có điểm dừng)

**Nguyên tắc**: Mỗi bước AI (denoise → OCR → LLM) là một **API riêng**, chạy độc lập, KHÔNG tự trigger bước kế tiếp. User quyết định.
","h2"
"Section:README.md:L397:7. Luồng nghiệp vụ end-to-end (pipeline có điểm dừng)","7. Luồng nghiệp vụ end-to-end (pipeline có điểm dừng)","README.md",397,439,2,"
## 7. Luồng nghiệp vụ end-to-end (pipeline có điểm dừng)

**Nguyên tắc**: Mỗi bước AI (denoise → OCR → LLM) là một **API riêng**, chạy độc lập, KHÔNG tự trigger bước kế tiếp. User quyết định.

### Bước 1 — Workspace
1. User login → GET `/documents` → render list workspace.
2. Chọn workspace cũ HOẶC `POST /documents` tạo mới.

### Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)
3. `POST /documents/{id}/pages/upload` → upload Cloudinary → ghi `pages(status='uploaded')` → enqueue `denoise_task`.
4. Worker: denoise → upload ảnh denoised → `pages.status='denoised'` (KHÔNG tự chạy OCR).
5. Frontend poll/WebSocket → biết denoise xong → hiển thị ảnh trước/sau.

### Bước 3 — User QUYẾT ĐỊNH:
   - **Option A — Download và dừng**: `GET /pages/{id}/download?type=denoised` → trả file ảnh denoised → user lưu về máy → KẾT THÚC luồng cho page này. `pages.status` vẫn là `denoised`, có thể quay lại chạy OCR sau.
   - **Option B — Chạy OCR**: `POST /pages/{id}/ocr` → enqueue `ocr_task`.

### Bước 4 — OCR (chỉ chạy khi user yêu cầu)
6. Worker chạy Tesseract → ghi `ocr_words` (text, confidence, bbox, is_suspicious) → `pages.status='ocr_done'`.
7. Frontend GET `/pages/{id}/ocr` → hiển thị text + highlight các từ có `is_suspicious=true` (confidence thấp).

### Bước 5 — User QUYẾT ĐỊNH:
   - **Option A — OCR đã ổn, bỏ qua LLM**: user có thể bấm Export ngay (xem bước 7).
   - **Option B — Gọi LLM correction**: `POST /pages/{id}/llm-correction` → backend chọn provider theo role (ChatGPT/Qwen free) → enqueue `llm_correction_task`.

### Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)
8. Worker: SuspiciousDetector → build chunks → LangChain `chain.batch()` → ghi `corrections(status='pending')` → `pages.status='llm_done'`.
9. Frontend GET `/pages/{id}/corrections?status=pending` → render diff inline.
10. User review từng đề xuất → `POST /corrections/{id}/keep` hoặc `/undo` (hoặc bulk).

### Bước 7 — Export (tùy chọn, chỉ User/Admin)
11. `POST /documents/{id}/exports {format:'docx'|'pdf'}` → backend build final text từ `ocr_words` + corrections `kept` (nếu có) → python-docx → docx2pdf → upload Cloudinary → trả URL.

### Bước 8 — Multi-workspace
12. User có thể mở workspace khác (tab mới); state mỗi workspace lưu trên row `documents.ui_state` (page đang xem, bước đang ở, filter…), không lẫn vào nhau.

### Ràng buộc với Viewer
- Bước 6 vẫn chạy được nhưng provider = Qwen free.
- Bước 7 trả 403.
- Bước 7 GET text trả về kèm `blurred=true` → frontend phủ blur.

---

## 8. Ba Sơ đồ thiết kế

### 8.1. USE CASE DIAGRAM
","h2"
"Section:README.md:L401:Bước 1 — Workspace","Bước 1 — Workspace","README.md",401,404,3,"
### Bước 1 — Workspace
1. User login → GET `/documents` → render list workspace.
2. Chọn workspace cũ HOẶC `POST /documents` tạo mới.

### Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)
3. `POST /documents/{id}/pages/upload` → upload Cloudinary → ghi `pages(status='uploaded')` → enqueue `denoise_task`.
4. Worker: denoise → upload ảnh denoised → `pages.status='denoised'` (KHÔNG tự chạy OCR).
","h3"
"Section:README.md:L405:Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)","Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)","README.md",405,409,3,"
### Bước 2 — Upload + Denoise (bắt buộc với mọi ảnh)
3. `POST /documents/{id}/pages/upload` → upload Cloudinary → ghi `pages(status='uploaded')` → enqueue `denoise_task`.
4. Worker: denoise → upload ảnh denoised → `pages.status='denoised'` (KHÔNG tự chạy OCR).
5. Frontend poll/WebSocket → biết denoise xong → hiển thị ảnh trước/sau.

### Bước 3 — User QUYẾT ĐỊNH:
   - **Option A — Download và dừng**: `GET /pages/{id}/download?type=denoised` → trả file ảnh denoised → user lưu về máy → KẾT THÚC luồng cho page này. `pages.status` vẫn là `denoised`, có thể quay lại chạy OCR sau.
   - **Option B — Chạy OCR**: `POST /pages/{id}/ocr` → enqueue `ocr_task`.
","h3"
"Section:README.md:L410:Bước 3 — User QUYẾT ĐỊNH:","Bước 3 — User QUYẾT ĐỊNH:","README.md",410,413,3,"
### Bước 3 — User QUYẾT ĐỊNH:
   - **Option A — Download và dừng**: `GET /pages/{id}/download?type=denoised` → trả file ảnh denoised → user lưu về máy → KẾT THÚC luồng cho page này. `pages.status` vẫn là `denoised`, có thể quay lại chạy OCR sau.
   - **Option B — Chạy OCR**: `POST /pages/{id}/ocr` → enqueue `ocr_task`.

### Bước 4 — OCR (chỉ chạy khi user yêu cầu)
6. Worker chạy Tesseract → ghi `ocr_words` (text, confidence, bbox, is_suspicious) → `pages.status='ocr_done'`.
7. Frontend GET `/pages/{id}/ocr` → hiển thị text + highlight các từ có `is_suspicious=true` (confidence thấp).
","h3"
"Section:README.md:L414:Bước 4 — OCR (chỉ chạy khi user yêu cầu)","Bước 4 — OCR (chỉ chạy khi user yêu cầu)","README.md",414,417,3,"
### Bước 4 — OCR (chỉ chạy khi user yêu cầu)
6. Worker chạy Tesseract → ghi `ocr_words` (text, confidence, bbox, is_suspicious) → `pages.status='ocr_done'`.
7. Frontend GET `/pages/{id}/ocr` → hiển thị text + highlight các từ có `is_suspicious=true` (confidence thấp).

### Bước 5 — User QUYẾT ĐỊNH:
   - **Option A — OCR đã ổn, bỏ qua LLM**: user có thể bấm Export ngay (xem bước 7).
   - **Option B — Gọi LLM correction**: `POST /pages/{id}/llm-correction` → backend chọn provider theo role (ChatGPT/Qwen free) → enqueue `llm_correction_task`.
","h3"
"Section:README.md:L418:Bước 5 — User QUYẾT ĐỊNH:","Bước 5 — User QUYẾT ĐỊNH:","README.md",418,421,3,"
### Bước 5 — User QUYẾT ĐỊNH:
   - **Option A — OCR đã ổn, bỏ qua LLM**: user có thể bấm Export ngay (xem bước 7).
   - **Option B — Gọi LLM correction**: `POST /pages/{id}/llm-correction` → backend chọn provider theo role (ChatGPT/Qwen free) → enqueue `llm_correction_task`.

### Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)
8. Worker: SuspiciousDetector → build chunks → LangChain `chain.batch()` → ghi `corrections(status='pending')` → `pages.status='llm_done'`.
9. Frontend GET `/pages/{id}/corrections?status=pending` → render diff inline.
","h3"
"Section:README.md:L422:Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)","Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)","README.md",422,426,3,"
### Bước 6 — LLM Correction (chỉ chạy khi user yêu cầu)
8. Worker: SuspiciousDetector → build chunks → LangChain `chain.batch()` → ghi `corrections(status='pending')` → `pages.status='llm_done'`.
9. Frontend GET `/pages/{id}/corrections?status=pending` → render diff inline.
10. User review từng đề xuất → `POST /corrections/{id}/keep` hoặc `/undo` (hoặc bulk).

### Bước 7 — Export (tùy chọn, chỉ User/Admin)
11. `POST /documents/{id}/exports {format:'docx'|'pdf'}` → backend build final text từ `ocr_words` + corrections `kept` (nếu có) → python-docx → docx2pdf → upload Cloudinary → trả URL.

","h3"
"Section:README.md:L427:Bước 7 — Export (tùy chọn, chỉ User/Admin)","Bước 7 — Export (tùy chọn, chỉ User/Admin)","README.md",427,429,3,"
### Bước 7 — Export (tùy chọn, chỉ User/Admin)
11. `POST /documents/{id}/exports {format:'docx'|'pdf'}` → backend build final text từ `ocr_words` + corrections `kept` (nếu có) → python-docx → docx2pdf → upload Cloudinary → trả URL.

### Bước 8 — Multi-workspace
12. User có thể mở workspace khác (tab mới); state mỗi workspace lưu trên row `documents.ui_state` (page đang xem, bước đang ở, filter…), không lẫn vào nhau.

","h3"
"Section:README.md:L430:Bước 8 — Multi-workspace","Bước 8 — Multi-workspace","README.md",430,432,3,"
### Bước 8 — Multi-workspace
12. User có thể mở workspace khác (tab mới); state mỗi workspace lưu trên row `documents.ui_state` (page đang xem, bước đang ở, filter…), không lẫn vào nhau.

### Ràng buộc với Viewer
- Bước 6 vẫn chạy được nhưng provider = Qwen free.
- Bước 7 trả 403.
","h3"
"Section:README.md:L433:Ràng buộc với Viewer","Ràng buộc với Viewer","README.md",433,439,3,"
### Ràng buộc với Viewer
- Bước 6 vẫn chạy được nhưng provider = Qwen free.
- Bước 7 trả 403.
- Bước 7 GET text trả về kèm `blurred=true` → frontend phủ blur.

---

## 8. Ba Sơ đồ thiết kế

### 8.1. USE CASE DIAGRAM
","h3"
"Section:README.md:L440:8. Ba Sơ đồ thiết kế","8. Ba Sơ đồ thiết kế","README.md",440,749,2,"
## 8. Ba Sơ đồ thiết kế

### 8.1. USE CASE DIAGRAM

Lưu ý: dùng quan hệ **«extend»** (tùy chọn) cho OCR và LLM vì user chủ động quyết định có chạy hay không; chỉ Upload→Denoise là «include» (luôn đi cùng nhau).

```mermaid
flowchart LR
    subgraph Actors
        V((Viewer))
        U((User))
        A((Admin))
    end

    subgraph ""Hệ thống Denoise + OCR + LLM""
        UC1([Đăng ký / Đăng nhập])
        UC2([Tạo / Mở Workspace])
        UC3([Upload trang ảnh])
        UC4([Khử nhiễu ảnh])
        UCDL([Download ảnh denoised<br/>về máy & dừng])
        UC5([Chạy OCR Tesseract])
        UC6([LLM Correction - Qwen free])
        UC7([LLM Correction - ChatGPT])
        UC8([Review Keep/Undo correction])
        UC9([Xem văn bản phục hồi])
        UC10([Xuất Word])
        UC11([Xuất PDF])
        UC12([Xem lịch sử cá nhân])
        UC13([Nâng cấp trả phí])
        UC14([Quản lý người dùng])
        UC15([Xem lịch sử toàn hệ thống])
        UC16([Quản lý gói + xem doanh thu])
        UC17([Văn bản hiển thị BLUR])
    end

    V --> UC1
    V --> UC2
    V --> UC3
    V --> UC4
    V --> UCDL
    V --> UC5
    V --> UC6
    V --> UC8
    V --> UC9
    V --> UC12
    V --> UC13
    UC9 -. include .-> UC17

    U --> UC1
    U --> UC2
    U --> UC3
    U --> UC4
    U --> UCDL
    U --> UC5
    U --> UC7
    U --> UC8
    U --> UC9
    U --> UC10
    U --> UC11
    U --> UC12

    A --> UC1
    A --> UC14
    A --> UC15
    A --> UC16

    UC3 -. include .-> UC4
    UC4 -. ""«extend» (user chọn)"" .-> UCDL
    UC4 -. ""«extend» (user chọn)"" .-> UC5
    UC5 -. ""«extend» (user chọn)"" .-> UC6
    UC5 -. ""«extend» (user chọn)"" .-> UC7
    UC6 -. include .-> UC8
    UC7 -. include .-> UC8
    UC10 -. extend .-> UC11
```

### 8.2. SEQUENCE DIAGRAM — Pipeline có điểm dừng cho user quyết định

```mermaid
sequenceDiagram
    autonumber
    actor U as User
    participant FE as Frontend
    participant API as FastAPI
    participant CDN as Cloudinary
    participant Q as Celery+Redis
    participant W as Worker
    participant DB as PostgreSQL
    participant LLM as LangChain Chain<br/>(OpenAI / OpenRouter)

    rect rgb(235,245,255)
    Note over U,DB: BƯỚC 1 — Upload + Denoise (luôn chạy)
    U->>FE: Chọn ảnh upload vào workspace
    FE->>API: POST /documents/{id}/pages/upload
    API->>CDN: Upload ảnh gốc
    CDN-->>API: original_url
    API->>DB: INSERT pages(status='uploaded')
    API->>Q: enqueue denoise_task
    API-->>FE: 202 {page_id}
    W->>DB: UPDATE status='denoising'
    W->>W: Run denoise model
    W->>CDN: Upload ảnh denoised
    W->>DB: UPDATE denoised_url, status='denoised'
    FE->>API: GET /pages/{id}/status (poll/WS)
    API-->>FE: status='denoised'
    FE->>U: Hiển thị ảnh trước/sau<br/>+ 2 nút: [Download] / [Run OCR]
    end

    alt OPTION A — User chọn Download và dừng
        U->>FE: Bấm Download
        FE->>API: GET /pages/{id}/download?type=denoised
        API->>CDN: Get denoised file
        API-->>FE: file blob
        FE->>U: Lưu vào máy
        Note over U,DB: LUỒNG KẾT THÚC tại đây
    else OPTION B — User chọn Run OCR
        rect rgb(245,255,235)
        Note over U,DB: BƯỚC 2 — OCR (chỉ khi user yêu cầu)
        U->>FE: Bấm Run OCR
        FE->>API: POST /pages/{id}/ocr
        API->>Q: enqueue ocr_task
        API-->>FE: 202
        W->>DB: UPDATE status='ocr_running'
        W->>W: Tesseract OCR
        W->>DB: INSERT ocr_words(text, confidence, bbox, is_suspicious)
        W->>DB: UPDATE status='ocr_done'
        FE->>API: GET /pages/{id}/ocr
        API-->>FE: words + highlight suspicious
        FE->>U: Hiển thị text<br/>+ 2 nút: [Looks Good → Export] / [Run LLM Correction]
        end

        alt User thấy OCR ổn → bỏ qua LLM
            Note over U,FE: Đi thẳng tới BƯỚC 4 Export
        else User muốn LLM sửa lỗi
            rect rgb(255,245,235)
            Note over U,DB: BƯỚC 3 — LLM Correction (chỉ khi user yêu cầu)
            U->>FE: Bấm Run LLM Correction
            FE->>API: POST /pages/{id}/llm-correction
            API->>API: get_llm_provider_for_user(role)
            API->>Q: enqueue llm_correction_task
            W->>DB: UPDATE status='llm_running'
            W->>W: SuspiciousDetector → chunks
            W->>LLM: chain.batch(chunks)
            LLM-->>W: List[CorrectionSuggestion]
            W->>DB: INSERT corrections(status='pending')
            W->>DB: UPDATE status='llm_done'
            FE->>API: GET /pages/{id}/corrections?status=pending
            API-->>FE: render diff inline
            loop Mỗi đề xuất
                U->>FE: Bấm Keep / Undo
                FE->>API: POST /corrections/{id}/keep|undo
                API->>DB: UPDATE corrections.status
            end
            end
        end

        rect rgb(255,235,245)
        Note over U,DB: BƯỚC 4 — Export (User/Admin only)
        U->>F
... [truncated]","h2"
"Section:README.md:L442:8.1. USE CASE DIAGRAM","8.1. USE CASE DIAGRAM","README.md",442,515,3,"
### 8.1. USE CASE DIAGRAM

Lưu ý: dùng quan hệ **«extend»** (tùy chọn) cho OCR và LLM vì user chủ động quyết định có chạy hay không; chỉ Upload→Denoise là «include» (luôn đi cùng nhau).

```mermaid
flowchart LR
    subgraph Actors
        V((Viewer))
        U((User))
        A((Admin))
    end

    subgraph ""Hệ thống Denoise + OCR + LLM""
        UC1([Đăng ký / Đăng nhập])
        UC2([Tạo / Mở Workspace])
        UC3([Upload trang ảnh])
        UC4([Khử nhiễu ảnh])
        UCDL([Download ảnh denoised<br/>về máy & dừng])
        UC5([Chạy OCR Tesseract])
        UC6([LLM Correction - Qwen free])
        UC7([LLM Correction - ChatGPT])
        UC8([Review Keep/Undo correction])
        UC9([Xem văn bản phục hồi])
        UC10([Xuất Word])
        UC11([Xuất PDF])
        UC12([Xem lịch sử cá nhân])
        UC13([Nâng cấp trả phí])
        UC14([Quản lý người dùng])
        UC15([Xem lịch sử toàn hệ thống])
        UC16([Quản lý gói + xem doanh thu])
        UC17([Văn bản hiển thị BLUR])
    end

    V --> UC1
    V --> UC2
    V --> UC3
    V --> UC4
    V --> UCDL
    V --> UC5
    V --> UC6
    V --> UC8
    V --> UC9
    V --> UC12
    V --> UC13
    UC9 -. include .-> UC17

    U --> UC1
    U --> UC2
    U --> UC3
    U --> UC4
    U --> UCDL
    U --> UC5
    U --> UC7
    U --> UC8
    U --> UC9
    U --> UC10
    U --> UC11
    U --> UC12

    A --> UC1
    A --> UC14
    A --> UC15
    A --> UC16

    UC3 -. include .-> UC4
    UC4 -. ""«extend» (user chọn)"" .-> UCDL
    UC4 -. ""«extend» (user chọn)"" .-> UC5
    UC5 -. ""«extend» (user chọn)"" .-> UC6
    UC5 -. ""«extend» (user chọn)"" .-> UC7
    UC6 -. include .-> UC8
    UC7 -. include .-> UC8
    UC10 -. extend .-> UC11
```

### 8.2. SEQUENCE DIAGRAM — Pipeline có điểm dừng cho user quyết định

```mermaid
","h3"
"Section:README.md:L516:8.2. SEQUENCE DIAGRAM — Pipeline có điểm dừng cho user quyết định","8.2. SEQUENCE DIAGRAM — Pipeline có điểm dừng cho user quyết định","README.md",516,609,3,"
### 8.2. SEQUENCE DIAGRAM — Pipeline có điểm dừng cho user quyết định

```mermaid
sequenceDiagram
    autonumber
    actor U as User
    participant FE as Frontend
    participant API as FastAPI
    participant CDN as Cloudinary
    participant Q as Celery+Redis
    participant W as Worker
    participant DB as PostgreSQL
    participant LLM as LangChain Chain<br/>(OpenAI / OpenRouter)

    rect rgb(235,245,255)
    Note over U,DB: BƯỚC 1 — Upload + Denoise (luôn chạy)
    U->>FE: Chọn ảnh upload vào workspace
    FE->>API: POST /documents/{id}/pages/upload
    API->>CDN: Upload ảnh gốc
    CDN-->>API: original_url
    API->>DB: INSERT pages(status='uploaded')
    API->>Q: enqueue denoise_task
    API-->>FE: 202 {page_id}
    W->>DB: UPDATE status='denoising'
    W->>W: Run denoise model
    W->>CDN: Upload ảnh denoised
    W->>DB: UPDATE denoised_url, status='denoised'
    FE->>API: GET /pages/{id}/status (poll/WS)
    API-->>FE: status='denoised'
    FE->>U: Hiển thị ảnh trước/sau<br/>+ 2 nút: [Download] / [Run OCR]
    end

    alt OPTION A — User chọn Download và dừng
        U->>FE: Bấm Download
        FE->>API: GET /pages/{id}/download?type=denoised
        API->>CDN: Get denoised file
        API-->>FE: file blob
        FE->>U: Lưu vào máy
        Note over U,DB: LUỒNG KẾT THÚC tại đây
    else OPTION B — User chọn Run OCR
        rect rgb(245,255,235)
        Note over U,DB: BƯỚC 2 — OCR (chỉ khi user yêu cầu)
        U->>FE: Bấm Run OCR
        FE->>API: POST /pages/{id}/ocr
        API->>Q: enqueue ocr_task
        API-->>FE: 202
        W->>DB: UPDATE status='ocr_running'
        W->>W: Tesseract OCR
        W->>DB: INSERT ocr_words(text, confidence, bbox, is_suspicious)
        W->>DB: UPDATE status='ocr_done'
        FE->>API: GET /pages/{id}/ocr
        API-->>FE: words + highlight suspicious
        FE->>U: Hiển thị text<br/>+ 2 nút: [Looks Good → Export] / [Run LLM Correction]
        end

        alt User thấy OCR ổn → bỏ qua LLM
            Note over U,FE: Đi thẳng tới BƯỚC 4 Export
        else User muốn LLM sửa lỗi
            rect rgb(255,245,235)
            Note over U,DB: BƯỚC 3 — LLM Correction (chỉ khi user yêu cầu)
            U->>FE: Bấm Run LLM Correction
            FE->>API: POST /pages/{id}/llm-correction
            API->>API: get_llm_provider_for_user(role)
            API->>Q: enqueue llm_correction_task
            W->>DB: UPDATE status='llm_running'
            W->>W: SuspiciousDetector → chunks
            W->>LLM: chain.batch(chunks)
            LLM-->>W: List[CorrectionSuggestion]
            W->>DB: INSERT corrections(status='pending')
            W->>DB: UPDATE status='llm_done'
            FE->>API: GET /pages/{id}/corrections?status=pending
            API-->>FE: render diff inline
            loop Mỗi đề xuất
                U->>FE: Bấm Keep / Undo
                FE->>API: POST /corrections/{id}/keep|undo
                API->>DB: UPDATE corrections.status
            end
            end
        end

        rect rgb(255,235,245)
        Note over U,DB: BƯỚC 4 — Export (User/Admin only)
        U->>FE: Bấm Export Word/PDF
        FE->>API: POST /documents/{id}/exports
        API->>API: Check role != viewer (else 403)
        API->>DB: Build final text = ocr_words + kept corrections
        API->>API: python-docx → docx2pdf
        API->>CDN: Upload file
        API->>DB: INSERT exports
        API-->>FE: file_url
        end
    end
```

### 8.3. ERD — Entity Relationship Diagram

```mermaid
","h3"
"Section:README.md:L610:8.3. ERD — Entity Relationship Diagram","8.3. ERD — Entity Relationship Diagram","README.md",610,749,3,"
### 8.3. ERD — Entity Relationship Diagram

```mermaid
erDiagram
    USERS ||--o{ REFRESH_TOKENS : has
    USERS ||--o{ DOCUMENTS : owns
    USERS ||--o{ USER_SUBSCRIPTIONS : subscribes
    USERS ||--o{ PAYMENTS : makes
    USERS ||--o{ AUDIT_LOGS : performs
    USERS ||--o{ FEEDBACK : writes
    USERS ||--o{ EXPORTS : creates
    USERS }o--|| DOCUMENTS : ""active_workspace""

    DOCUMENTS ||--o{ PAGES : contains
    DOCUMENTS ||--o{ FEEDBACK : has
    DOCUMENTS ||--o{ EXPORTS : produces

    PAGES ||--o{ OCR_WORDS : ""OCR yields""
    PAGES ||--o{ CORRECTIONS : ""LLM suggests""
    OCR_WORDS ||--o{ CORRECTIONS : ""may correct""

    SUBSCRIPTION_PLANS ||--o{ USER_SUBSCRIPTIONS : ""is chosen by""
    USER_SUBSCRIPTIONS ||--o{ PAYMENTS : ""billed via""

    USERS {
        uuid id PK
        string email UK
        string password_hash
        enum role ""admin|user|viewer""
        enum status
        int images_used
        uuid active_workspace_id FK
        timestamp created_at
    }

    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        enum status ""draft|processing|ready|archived""
        int total_pages
        jsonb ui_state
        timestamp last_opened_at
    }

    PAGES {
        uuid id PK
        uuid document_id FK
        int page_number
        string original_url
        string denoised_url
        enum status
    }

    OCR_WORDS {
        bigint id PK
        uuid page_id FK
        int word_index
        text text
        numeric confidence
        jsonb bbox
        bool is_suspicious
    }

    CORRECTIONS {
        uuid id PK
        uuid page_id FK
        bigint ocr_word_id FK
        int_array word_indices
        text original_text
        text suggested_text
        enum llm_provider ""openai|openrouter_qwen""
        string llm_model
        enum status ""pending|kept|undone""
        timestamp reviewed_at
        uuid reviewed_by FK
    }

    FEEDBACK {
        uuid id PK
        uuid document_id FK
        uuid page_id FK
        uuid user_id FK
        text note
    }

    EXPORTS {
        uuid id PK
        uuid document_id FK
        uuid user_id FK
        enum format ""docx|pdf""
        string file_url
    }

    SUBSCRIPTION_PLANS {
        int id PK
        string code UK
        string name
        int price_vnd
        int duration_days
    }

    USER_SUBSCRIPTIONS {
        uuid id PK
        uuid user_id FK
        int plan_id FK
        enum status
        timestamp starts_at
        timestamp ends_at
    }

    PAYMENTS {
        uuid id PK
        uuid user_id FK
        uuid subscription_id FK
        enum gateway ""vnpay|momo|stripe""
        string gateway_txn_id UK
        int amount_vnd
        enum status
    }

    REFRESH_TOKENS {
        uuid id PK
        uuid user_id FK
        string token_hash
        timestamp expires_at
    }

    AUDIT_LOGS {
        bigint id PK
        uuid actor_id FK
        string action
        string target_type
        string target_id
        jsonb metadata
    }
```

---

## 9. Bảo mật & lưu ý

- JWT claim chứa `user_id`, `role`. Đổi role buộc revoke refresh token.
","h3"
"Section:README.md:L750:9. Bảo mật & lưu ý","9. Bảo mật & lưu ý","README.md",750,761,2,"
## 9. Bảo mật & lưu ý

- JWT claim chứa `user_id`, `role`. Đổi role buộc revoke refresh token.
- LLM API key (OpenAI, OpenRouter) chỉ ở backend, không lộ ra client.
- Backend tự chọn provider theo role — không cho client truyền `provider` parameter (tránh viewer lách dùng ChatGPT).
- Webhook payment verify HMAC + idempotency UNIQUE `gateway_txn_id`.
- Defense in depth cho Viewer: blur frontend + 403 backend ở `/exports/*`.
- `corrections.suggested_text` không lưu PII của LLM call (raw prompt log chỉ giữ 30 ngày).
- Rate limit upload + LLM call per user (slowapi).

---

## 10. Verification

1. `alembic upgrade head` — tạo đủ 12 bảng.
","h2"
"Section:README.md:L762:10. Verification","10. Verification","README.md",762,776,2,"
## 10. Verification

1. `alembic upgrade head` — tạo đủ 12 bảng.
2. Seed admin/user/viewer + 2 plan.
3. **Test Viewer**: upload 10 page OK, page 11 trả 403; LLM trả về dùng Qwen free; `/exports/pdf` 403.
4. **Test User**: full flow upload → denoise → OCR → LLM ChatGPT → keep/undo → export PDF mở được; corrections.status đúng.
5. **Test Multi-workspace**: tạo 2 document, mỗi cái upload 1 page, switch qua lại — `ui_state` lưu/khôi phục đúng; corrections của workspace A không lẫn workspace B.
6. **Test LangChain**: mock LLM trả JSON cố định → chain parse Pydantic OK → corrections insert đủ.
7. **Test payment sandbox** → role đổi viewer→user, `images_used` reset.
8. **Test expire subscription** → downgrade về viewer.
9. Unit test `suspicious_detector_service` với threshold khác nhau.
10. Unit test `correction_review_service.build_final_text()` với mix kept/undone/pending.

---

## 11. Phạm vi KHÔNG nằm trong plan này

- Thiết kế UI/UX chi tiết (font, màu, layout từng màn).
","h2"
"Section:README.md:L777:11. Phạm vi KHÔNG nằm trong plan này","11. Phạm vi KHÔNG nằm trong plan này","README.md",777,783,2,"
## 11. Phạm vi KHÔNG nằm trong plan này

- Thiết kế UI/UX chi tiết (font, màu, layout từng màn).
- Quyết định chính xác model denoise (sẽ POC).
- CI/CD, Docker, K8s.
- Đa ngôn ngữ giao diện.
","h2"
"Section:backend/Readme_backend.md:L3:1. Hạ tầng (Postgres + Redis)","1. Hạ tầng (Postgres + Redis)","backend/Readme_backend.md",3,5,1,"
# 1. Hạ tầng (Postgres + Redis)
docker compose up -d

# 2. Tạo file cấu hình local
Copy-Item .env.example .env
","h1"
"Section:backend/Readme_backend.md:L6:2. Tạo file cấu hình local","2. Tạo file cấu hình local","backend/Readme_backend.md",6,8,1,"
# 2. Tạo file cấu hình local
Copy-Item .env.example .env

# 3. Môi trường Python (bạn tự cài)
python -m venv .venv
.venv\Scripts\Activate.ps1","h1"
"Section:backend/Readme_backend.md:L9:3. Môi trường Python (bạn tự cài)","3. Môi trường Python (bạn tự cài)","backend/Readme_backend.md",9,13,1,"
# 3. Môi trường Python (bạn tự cài)
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt    # tensorflow ~1GB, tải lâu

# 4. Tạo bảng
alembic upgrade head
","h1"
"Section:backend/Readme_backend.md:L14:4. Tạo bảng","4. Tạo bảng","backend/Readme_backend.md",14,16,1,"
# 4. Tạo bảng
alembic upgrade head

# 5. Chạy API
uvicorn app.main:app --reload
","h1"
"Section:backend/Readme_backend.md:L17:5. Chạy API","5. Chạy API","backend/Readme_backend.md",17,19,1,"
# 5. Chạy API
uvicorn app.main:app --reload
","h1"
"Section:backend/docs/upload_denoise_review.md:L1:Review: Upload ảnh + Khử nhiễu (Backend)","Review: Upload ảnh + Khử nhiễu (Backend)","backend/docs/upload_denoise_review.md",1,242,1,"# Review: Upload ảnh + Khử nhiễu (Backend)

Tài liệu này tổng kết phần code đã viết để hoàn thiện luồng **upload ảnh lên Cloudinary → khử nhiễu bằng model U-Net → trả ảnh đã restore** trên backend, cùng các quyết định thiết kế đi kèm để bạn dễ đọc lại và tiếp tục phát triển.

---

## 1. Mục tiêu

Cho phép một user đã đăng nhập:

1. Tạo workspace (document).
2. Upload ảnh tài liệu (jpg/png/...) vào workspace.
3. Hệ thống tự khử nhiễu ảnh và lưu kết quả lên Cloudinary.
4. User xem trạng thái + tải về ảnh đã khử nhiễu.

Vẫn giữ nguyên thiết kế ""pipeline có điểm dừng"": **upload → denoise** là tự động; OCR và LLM correction sẽ là các action riêng do user chủ động gọi sau.

---

## 2. Luồng end-to-end

```
[Client]                  [FastAPI]                 [Cloudinary]      [TensorFlow]      [PostgreSQL]
   │                          │                          │                  │                  │
   │ POST /auth/dev-login     │                          │                  │                  │
   │─────────────────────────►│                          │                  │                  │
   │◄──── access_token ───────│                          │                  │                  │
   │                          │                          │                  │                  │
   │ POST /documents          │                          │                  │                  │
   │─────────────────────────►│─── INSERT documents ────────────────────────────────────────►│
   │◄──── doc_id ─────────────│                                                              │
   │                          │                                                              │
   │ POST /documents/{id}/    │                                                              │
   │     pages/upload (file)  │                                                              │
   │─────────────────────────►│                                                              │
   │                          │── upload bytes ────────►│                                    │
   │                          │◄─── url, public_id ─────│                                    │
   │                          │── INSERT pages ─────────────────────────────────────────────►│
   │                          │                                                              │
   │                          │   denoise_page_task.delay(page_id)                           │
   │                          │   (CELERY_TASK_ALWAYS_EAGER=true ⇒ chạy đồng bộ tại đây)     │
   │                          │                                                              │
   │                          │── UPDATE status=denoising ──────────────────────────────────►│
   │                          │── download original ───►│                                    │
   │                          │◄── bytes ───────────────│                                    │
   │                          │── load_model() + run_inference() ────────►│                  │
   │                          │◄── denoised bytes ────────────────────────│                  │
   │                          │── upload denoised ─────►│                                    │
   │                          │◄── denoised_url ────────│                                    │
   │                          │── UPDATE denoised_url, status=denoised ────────────────────►│
   │◄── 202 PageOut ──────────│                                                              │
   │                          │                                                              │
   │ GET /pages/{id}/status   │── SELECT page ─────────────────────────────────────────────►│
   │◄── status=denoised ──────│                                                              │
   │                          │                                                              │
   │ GET /pages/{id}/download │                                                              │
   │─────────────────────────►│                                                              │
   │◄── 307 redirect ─────────│ (URL Cloudinary)                                             │
```

Khi `CELERY_TASK_ALWAYS_EAGER=false` thì `denoise_page_task.delay(...)` chỉ enqueue vào Redis và return ngay → response 202 trả về cho client; worker Celery riêng xử lý tiếp.

---

## 3. Cây file đã thêm / sửa

```
backend/
├── docker-compose.yml                  [NEW]   Postgres + Redis cho dev
├── requirements.txt                    [EDIT]  +tensorflow, +email-validator
├── .env / .env.example                 [EDIT]  +CELERY_TASK_ALWAYS_EAGER, +denoise/upload settings
│
├── alembic/versions/0001_initial.py    [EDIT]  enum dùng create_type=False (bug duplicate)
│
└── app/
    ├── core/
    │   ├── config.py                   [EDIT]  +DENOISING_*, +MAX_UPLOAD_SIZE_MB, +ALLOWED_IMAGE_TYPES, ...
    │   └── logging.py                  [EDIT]  +get_logger()
    │
    ├── storage/                        [NEW]   Storage abstraction
    │   ├─
... [truncated]","h1"
"Section:backend/docs/upload_denoise_review.md:L7:1. Mục tiêu","1. Mục tiêu","backend/docs/upload_denoise_review.md",7,19,2,"
## 1. Mục tiêu

Cho phép một user đã đăng nhập:

1. Tạo workspace (document).
2. Upload ảnh tài liệu (jpg/png/...) vào workspace.
3. Hệ thống tự khử nhiễu ảnh và lưu kết quả lên Cloudinary.
4. User xem trạng thái + tải về ảnh đã khử nhiễu.

Vẫn giữ nguyên thiết kế ""pipeline có điểm dừng"": **upload → denoise** là tự động; OCR và LLM correction sẽ là các action riêng do user chủ động gọi sau.

---

## 2. Luồng end-to-end

```","h2"
"Section:backend/docs/upload_denoise_review.md:L20:2. Luồng end-to-end","2. Luồng end-to-end","backend/docs/upload_denoise_review.md",20,64,2,"
## 2. Luồng end-to-end

```
[Client]                  [FastAPI]                 [Cloudinary]      [TensorFlow]      [PostgreSQL]
   │                          │                          │                  │                  │
   │ POST /auth/dev-login     │                          │                  │                  │
   │─────────────────────────►│                          │                  │                  │
   │◄──── access_token ───────│                          │                  │                  │
   │                          │                          │                  │                  │
   │ POST /documents          │                          │                  │                  │
   │─────────────────────────►│─── INSERT documents ────────────────────────────────────────►│
   │◄──── doc_id ─────────────│                                                              │
   │                          │                                                              │
   │ POST /documents/{id}/    │                                                              │
   │     pages/upload (file)  │                                                              │
   │─────────────────────────►│                                                              │
   │                          │── upload bytes ────────►│                                    │
   │                          │◄─── url, public_id ─────│                                    │
   │                          │── INSERT pages ─────────────────────────────────────────────►│
   │                          │                                                              │
   │                          │   denoise_page_task.delay(page_id)                           │
   │                          │   (CELERY_TASK_ALWAYS_EAGER=true ⇒ chạy đồng bộ tại đây)     │
   │                          │                                                              │
   │                          │── UPDATE status=denoising ──────────────────────────────────►│
   │                          │── download original ───►│                                    │
   │                          │◄── bytes ───────────────│                                    │
   │                          │── load_model() + run_inference() ────────►│                  │
   │                          │◄── denoised bytes ────────────────────────│                  │
   │                          │── upload denoised ─────►│                                    │
   │                          │◄── denoised_url ────────│                                    │
   │                          │── UPDATE denoised_url, status=denoised ────────────────────►│
   │◄── 202 PageOut ──────────│                                                              │
   │                          │                                                              │
   │ GET /pages/{id}/status   │── SELECT page ─────────────────────────────────────────────►│
   │◄── status=denoised ──────│                                                              │
   │                          │                                                              │
   │ GET /pages/{id}/download │                                                              │
   │─────────────────────────►│                                                              │
   │◄── 307 redirect ─────────│ (URL Cloudinary)                                             │
```

Khi `CELERY_TASK_ALWAYS_EAGER=false` thì `denoise_page_task.delay(...)` chỉ enqueue vào Redis và return ngay → response 202 trả về cho client; worker Celery riêng xử lý tiếp.

---

## 3. Cây file đã thêm / sửa

```","h2"
"Section:backend/docs/upload_denoise_review.md:L65:3. Cây file đã thêm / sửa","3. Cây file đã thêm / sửa","backend/docs/upload_denoise_review.md",65,107,2,"
## 3. Cây file đã thêm / sửa

```
backend/
├── docker-compose.yml                  [NEW]   Postgres + Redis cho dev
├── requirements.txt                    [EDIT]  +tensorflow, +email-validator
├── .env / .env.example                 [EDIT]  +CELERY_TASK_ALWAYS_EAGER, +denoise/upload settings
│
├── alembic/versions/0001_initial.py    [EDIT]  enum dùng create_type=False (bug duplicate)
│
└── app/
    ├── core/
    │   ├── config.py                   [EDIT]  +DENOISING_*, +MAX_UPLOAD_SIZE_MB, +ALLOWED_IMAGE_TYPES, ...
    │   └── logging.py                  [EDIT]  +get_logger()
    │
    ├── storage/                        [NEW]   Storage abstraction
    │   ├── __init__.py                 get_storage() factory
    │   ├── base.py                     Storage Protocol + StoredImage dataclass
    │   ├── cloudinary_backend.py       Cloudinary impl
    │   └── local_backend.py            Local-disk fallback
    │
    ├── services/
    │   └── denoise_service.py          [NEW]   Orchestration denoise cho 1 page
    │
    ├── workers/
    │   ├── celery_app.py               [NEW]   Celery app + eager-mode
    │   └── denoise_task.py             [NEW]   @task wrap service
    │
    ├── schemas/                        [NEW]   Pydantic I/O
    │   ├── auth.py                     DevLoginIn, TokenOut
    │   ├── document.py                 DocumentCreate, DocumentOut
    │   └── page.py                     PageOut, PageStatusOut
    │
    ├── api/
    │   ├── deps.py                     [EDIT]  HTTPBearer → Swagger Authorize button
    │   └── v1/endpoints/
    │       ├── auth.py                 [EDIT]  +POST /auth/dev-login
    │       ├── documents.py            [EDIT]  create/get/list/archive
    │       └── pages.py                [EDIT]  upload/status/download
```

---

## 4. Phân tích từng module

### 4.1 Storage abstraction (`app/storage/`)","h2"
"Section:backend/docs/upload_denoise_review.md:L108:4. Phân tích từng module","4. Phân tích từng module","backend/docs/upload_denoise_review.md",108,221,2,"
## 4. Phân tích từng module

### 4.1 Storage abstraction (`app/storage/`)

**Mục đích**: Tách logic lưu trữ ảnh ra khỏi business code, hỗ trợ 2 backend mà không sửa endpoint.

- [base.py](../app/storage/base.py)
  - `StoredImage` dataclass: chuẩn hoá thông tin trả về (`key`, `url`, `width`, `height`, `bytes_size`).
  - `Storage` Protocol (runtime-checkable): 3 method `upload_image / download / delete`.
- [cloudinary_backend.py](../app/storage/cloudinary_backend.py)
  - Config từ `settings.CLOUDINARY_*`.
  - `upload_image`: dùng `cloudinary.uploader.upload`, public_id theo dạng `documents/{doc_id}/(original|denoised)/page_{n}`.
  - `download`: nếu là URL `http(s)` thì `httpx.get`, nếu là public_id thì resolve qua `cloudinary.utils.cloudinary_url`.
- [local_backend.py](../app/storage/local_backend.py)
  - Fallback khi Cloudinary chưa cấu hình.
  - URL trả về có prefix `local://` để phân biệt với Cloudinary URL (endpoint download dùng prefix này để quyết định stream vs redirect).
- [\_\_init\_\_.py](../app/storage/__init__.py)
  - `get_storage()` cached: chọn Cloudinary nếu có `CLOUDINARY_CLOUD_NAME`, ngược lại local.

**Tại sao tách thế này**: dev có thể test mà không cần Cloudinary credentials (chạy local), production switch sang Cloudinary chỉ qua env vars. Không cần `if isinstance(...)` ở chỗ gọi.

---

### 4.2 Denoise service (`app/services/denoise_service.py`)

Hàm chính: `denoise_page(page_id)`.

**Trách nhiệm**:
1. Mở DB session riêng (vì có thể chạy trong Celery worker, ngoài context request).
2. Lazy-import TensorFlow + model_loader bên trong hàm — tránh ép API process load TF khi không denoise.
3. Set page.status = `denoising`.
4. Tải bytes ảnh gốc từ storage (key = `cloudinary_public_id`).
5. Ghi ra `tempfile.TemporaryDirectory` (vì `inference.run_inference` yêu cầu path file).
6. `load_model()` (LRU-cached, lần đầu chậm vì load weights `.h5`).
7. `run_inference(model, input_path, output_path)` — patch-based inference theo `DENOISING_IMAGE_SIZE`.
8. Upload bytes denoised lên storage.
9. Update `denoised_url`, `width`, `height`, `status=denoised`, `completed_at`.
10. Try/except: nếu fail thì rollback + set `status=failed`, ghi `processing_error`.

**Pattern quan trọng**: service tự quản lý session, không nhận từ ngoài → dùng được cả ở HTTP request, Celery worker, hay test script.

---

### 4.3 Celery (`app/workers/`)

[celery_app.py](../app/workers/celery_app.py):
- Broker + backend từ `CELERY_BROKER_URL` / `CELERY_RESULT_BACKEND` (Redis).
- `task_always_eager = settings.CELERY_TASK_ALWAYS_EAGER`: khi `true`, `.delay()` chạy đồng bộ in-process — **không cần Redis/worker để test**.
- `task_eager_propagates`: lỗi trong eager mode raise lên (thay vì nuốt) để dev thấy ngay.
- Import `denoise_task` ở cuối file để register task với app (Celery cần task được import để biết).

[denoise_task.py](../app/workers/denoise_task.py):
- `@celery_app.task(name=""denoise.page"", bind=True, max_retries=2, default_retry_delay=10)`.
- Chỉ là wrapper mỏng gọi `denoise_service.denoise_page(page_id)` → giữ logic ở service, dễ test không qua Celery.

---

### 4.4 Schemas (`app/schemas/`)

3 file Pydantic, mỗi file 1 domain:
- `auth.py`: `DevLoginIn(email, role)`, `TokenOut(access_token, token_type)`.
- `document.py`: `DocumentCreate(title, description)`, `DocumentOut` (config `from_attributes=True` để serialize từ ORM model).
- `page.py`: `PageOut` (đầy đủ), `PageStatusOut` (gọn, dùng cho polling status).

Re-export tập trung ở `__init__.py` cho gọn import.

---

### 4.5 API endpoints

[auth.py](../app/api/v1/endpoints/auth.py) — `POST /auth/dev-login`:
- Chặn ngoài `APP_ENV=development`.
- Get-or-create user theo email, gán role, trả `access_token`.
- Phục vụ test: không cần implement đầy đủ register/login mới chạy được luồng upload.

[documents.py](../app/api/v1/endpoints/documents.py):
- CRUD cơ bản workspace của owner.
- Viewer bị chặn ở `VIEWER_MAX_WORKSPACES` (default 2).
- DELETE = archive mềm (status = archived), không xoá thật để giữ lịch sử.

[pages.py](../app/api/v1/endpoints/pages.py) — phần trọng tâm:

```
POST /documents/{doc_id}/pages/upload     → 202 PageOut
GET  /pages/{page_id}/status              → PageStatusOut
GET  /pages/{page_id}/download?type=...   → 307 redirect hoặc stream
```

Upload flow:
1. Verify doc thuộc user (`NotFound` nếu không).
2. Validate `content_type` (theo `ALLOWED_IMAGE_TYPES`), size (`MAX_UPLOAD_SIZE_MB`).
3. Viewer: check `images_used < VIEWER_MAX_IMAGES`.
4. Tính `page_number = MAX(existing) + 1` (atomic-enough cho dev, có race nhưng UniqueConstraint sẽ catch).
5. Upload bytes lên storage.
6. Insert Page record; increment `document.total_pages`, `user.images_used`.
7. Enqueue denoise → response 202.
8. `db.refresh(page)` lấy state mới nhất (vì eager mode có thể đã update status trong cùng tx flow).

Download:
- Cloudinary URL (`http://...`) → `RedirectResponse` để client tải trực tiếp (đỡ băng thông backend).
- Local URL (`local://...`) → `StreamingResponse` từ bytes.

---

### 4.
... [truncated]","h2"
"Section:backend/docs/upload_denoise_review.md:L110:4.1 Storage abstraction (`app/storage/`)","4.1 Storage abstraction (`app/storage/`)","backend/docs/upload_denoise_review.md",110,130,3,"
### 4.1 Storage abstraction (`app/storage/`)

**Mục đích**: Tách logic lưu trữ ảnh ra khỏi business code, hỗ trợ 2 backend mà không sửa endpoint.

- [base.py](../app/storage/base.py)
  - `StoredImage` dataclass: chuẩn hoá thông tin trả về (`key`, `url`, `width`, `height`, `bytes_size`).
  - `Storage` Protocol (runtime-checkable): 3 method `upload_image / download / delete`.
- [cloudinary_backend.py](../app/storage/cloudinary_backend.py)
  - Config từ `settings.CLOUDINARY_*`.
  - `upload_image`: dùng `cloudinary.uploader.upload`, public_id theo dạng `documents/{doc_id}/(original|denoised)/page_{n}`.
  - `download`: nếu là URL `http(s)` thì `httpx.get`, nếu là public_id thì resolve qua `cloudinary.utils.cloudinary_url`.
- [local_backend.py](../app/storage/local_backend.py)
  - Fallback khi Cloudinary chưa cấu hình.
  - URL trả về có prefix `local://` để phân biệt với Cloudinary URL (endpoint download dùng prefix này để quyết định stream vs redirect).
- [\_\_init\_\_.py](../app/storage/__init__.py)
  - `get_storage()` cached: chọn Cloudinary nếu có `CLOUDINARY_CLOUD_NAME`, ngược lại local.

**Tại sao tách thế này**: dev có thể test mà không cần Cloudinary credentials (chạy local), production switch sang Cloudinary chỉ qua env vars. Không cần `if isinstance(...)` ở chỗ gọi.

---

### 4.2 Denoise service (`app/services/denoise_service.py`)

Hàm chính: `denoise_page(page_id)`.","h3"
"Section:backend/docs/upload_denoise_review.md:L131:4.2 Denoise service (`app/services/denoise_service.py`)","4.2 Denoise service (`app/services/denoise_service.py`)","backend/docs/upload_denoise_review.md",131,150,3,"
### 4.2 Denoise service (`app/services/denoise_service.py`)

Hàm chính: `denoise_page(page_id)`.

**Trách nhiệm**:
1. Mở DB session riêng (vì có thể chạy trong Celery worker, ngoài context request).
2. Lazy-import TensorFlow + model_loader bên trong hàm — tránh ép API process load TF khi không denoise.
3. Set page.status = `denoising`.
4. Tải bytes ảnh gốc từ storage (key = `cloudinary_public_id`).
5. Ghi ra `tempfile.TemporaryDirectory` (vì `inference.run_inference` yêu cầu path file).
6. `load_model()` (LRU-cached, lần đầu chậm vì load weights `.h5`).
7. `run_inference(model, input_path, output_path)` — patch-based inference theo `DENOISING_IMAGE_SIZE`.
8. Upload bytes denoised lên storage.
9. Update `denoised_url`, `width`, `height`, `status=denoised`, `completed_at`.
10. Try/except: nếu fail thì rollback + set `status=failed`, ghi `processing_error`.

**Pattern quan trọng**: service tự quản lý session, không nhận từ ngoài → dùng được cả ở HTTP request, Celery worker, hay test script.

---

### 4.3 Celery (`app/workers/`)

[celery_app.py](../app/workers/celery_app.py):","h3"
"Section:backend/docs/upload_denoise_review.md:L151:4.3 Celery (`app/workers/`)","4.3 Celery (`app/workers/`)","backend/docs/upload_denoise_review.md",151,164,3,"
### 4.3 Celery (`app/workers/`)

[celery_app.py](../app/workers/celery_app.py):
- Broker + backend từ `CELERY_BROKER_URL` / `CELERY_RESULT_BACKEND` (Redis).
- `task_always_eager = settings.CELERY_TASK_ALWAYS_EAGER`: khi `true`, `.delay()` chạy đồng bộ in-process — **không cần Redis/worker để test**.
- `task_eager_propagates`: lỗi trong eager mode raise lên (thay vì nuốt) để dev thấy ngay.
- Import `denoise_task` ở cuối file để register task với app (Celery cần task được import để biết).

[denoise_task.py](../app/workers/denoise_task.py):
- `@celery_app.task(name=""denoise.page"", bind=True, max_retries=2, default_retry_delay=10)`.
- Chỉ là wrapper mỏng gọi `denoise_service.denoise_page(page_id)` → giữ logic ở service, dễ test không qua Celery.

---

### 4.4 Schemas (`app/schemas/`)

3 file Pydantic, mỗi file 1 domain:","h3"
"Section:backend/docs/upload_denoise_review.md:L165:4.4 Schemas (`app/schemas/`)","4.4 Schemas (`app/schemas/`)","backend/docs/upload_denoise_review.md",165,175,3,"
### 4.4 Schemas (`app/schemas/`)

3 file Pydantic, mỗi file 1 domain:
- `auth.py`: `DevLoginIn(email, role)`, `TokenOut(access_token, token_type)`.
- `document.py`: `DocumentCreate(title, description)`, `DocumentOut` (config `from_attributes=True` để serialize từ ORM model).
- `page.py`: `PageOut` (đầy đủ), `PageStatusOut` (gọn, dùng cho polling status).

Re-export tập trung ở `__init__.py` cho gọn import.

---

### 4.5 API endpoints

[auth.py](../app/api/v1/endpoints/auth.py) — `POST /auth/dev-login`:","h3"
"Section:backend/docs/upload_denoise_review.md:L176:4.5 API endpoints","4.5 API endpoints","backend/docs/upload_denoise_review.md",176,211,3,"
### 4.5 API endpoints

[auth.py](../app/api/v1/endpoints/auth.py) — `POST /auth/dev-login`:
- Chặn ngoài `APP_ENV=development`.
- Get-or-create user theo email, gán role, trả `access_token`.
- Phục vụ test: không cần implement đầy đủ register/login mới chạy được luồng upload.

[documents.py](../app/api/v1/endpoints/documents.py):
- CRUD cơ bản workspace của owner.
- Viewer bị chặn ở `VIEWER_MAX_WORKSPACES` (default 2).
- DELETE = archive mềm (status = archived), không xoá thật để giữ lịch sử.

[pages.py](../app/api/v1/endpoints/pages.py) — phần trọng tâm:

```
POST /documents/{doc_id}/pages/upload     → 202 PageOut
GET  /pages/{page_id}/status              → PageStatusOut
GET  /pages/{page_id}/download?type=...   → 307 redirect hoặc stream
```

Upload flow:
1. Verify doc thuộc user (`NotFound` nếu không).
2. Validate `content_type` (theo `ALLOWED_IMAGE_TYPES`), size (`MAX_UPLOAD_SIZE_MB`).
3. Viewer: check `images_used < VIEWER_MAX_IMAGES`.
4. Tính `page_number = MAX(existing) + 1` (atomic-enough cho dev, có race nhưng UniqueConstraint sẽ catch).
5. Upload bytes lên storage.
6. Insert Page record; increment `document.total_pages`, `user.images_used`.
7. Enqueue denoise → response 202.
8. `db.refresh(page)` lấy state mới nhất (vì eager mode có thể đã update status trong cùng tx flow).

Download:
- Cloudinary URL (`http://...`) → `RedirectResponse` để client tải trực tiếp (đỡ băng thông backend).
- Local URL (`local://...`) → `StreamingResponse` từ bytes.

---

### 4.6 Auth dep (`app/api/deps.py`)

Trước: tự đọc `Header(""Authorization"")` → Swagger không biết khai báo security → không có nút Authorize.","h3"
"Section:backend/docs/upload_denoise_review.md:L212:4.6 Auth dep (`app/api/deps.py`)","4.6 Auth dep (`app/api/deps.py`)","backend/docs/upload_denoise_review.md",212,221,3,"
### 4.6 Auth dep (`app/api/deps.py`)

Trước: tự đọc `Header(""Authorization"")` → Swagger không biết khai báo security → không có nút Authorize.

Sau: dùng `fastapi.security.HTTPBearer(auto_error=False)` → FastAPI tự thêm `securitySchemes` vào OpenAPI → Swagger render nút Authorize → click 1 lần ghi nhớ token, tất cả request từ tab đó tự kèm `Authorization: Bearer <token>`.

`auto_error=False` để giữ tự do trả `AuthenticationError` (HTTP 401) đồng nhất với app, thay vì FastAPI tự raise 403.

---

## 5. Quyết định thiết kế đáng chú ý

| Vấn đề | Lựa chọn | Lý do |","h3"
"Section:backend/docs/upload_denoise_review.md:L222:5. Quyết định thiết kế đáng chú ý","5. Quyết định thiết kế đáng chú ý","backend/docs/upload_denoise_review.md",222,237,2,"
## 5. Quyết định thiết kế đáng chú ý

| Vấn đề | Lựa chọn | Lý do |
|---|---|---|
| Sync vs Celery | Celery + `ALWAYS_EAGER` cho dev | Code đúng kiến trúc thật, vẫn test được không cần worker chạy ngầm |
| Cloudinary creds chưa có khi dev | Fallback local-disk tự động | Dev mới clone repo vẫn chạy được, không cần đăng ký Cloudinary trước |
| Login chưa implement | Thêm `/auth/dev-login` chặn theo env | Test upload không bị chặn bởi auth chưa xong, không lộ ra prod |
| Storage interface | `Protocol` thay vì ABC | Duck typing, không cần inherit, hợp với 2 impl đơn giản |
| Lazy import TF | Trong hàm denoise, không top-level | API process khởi động không tốn 5-10s load TF; chỉ load khi denoise thật |
| `_LOCAL_PREFIX = ""local://""` | Phân biệt URL ở download endpoint | Tránh phải store thêm cờ ""backend_type""; URL self-describing |
| Migration enum `create_type=False` | Khai báo `postgresql.ENUM(..., create_type=False)` | Bug duplicate khi 2 enum object dùng cùng tên type; chỉ vòng `enum.create(checkfirst=True)` tạo |
| Postgres port 5433 | Tránh xung đột với project `market_agent_db` | Cho phép nhiều project Postgres chạy song song trên 1 máy |
| Compose project name = `denoise` | `name: denoise` đặt cứng | Không phụ thuộc tên thư mục → hiển thị Docker Desktop nhất quán |

---

## 6. Cách test

```powershell","h2"
"Section:backend/docs/upload_denoise_review.md:L238:6. Cách test","6. Cách test","backend/docs/upload_denoise_review.md",238,242,2,"
## 6. Cách test

```powershell
cd ""d:\Học tập\Nam04\Denoise_project\backend""

# Hạ tầng
docker compose up -d
","h2"
"Section:backend/docs/upload_denoise_review.md:L243:Hạ tầng","Hạ tầng","backend/docs/upload_denoise_review.md",243,245,1,"
# Hạ tầng
docker compose up -d

# Python env (chỉ làm 1 lần)
python -m venv .venv
.venv\Scripts\Activate.ps1","h1"
"Section:backend/docs/upload_denoise_review.md:L246:Python env (chỉ làm 1 lần)","Python env (chỉ làm 1 lần)","backend/docs/upload_denoise_review.md",246,250,1,"
# Python env (chỉ làm 1 lần)
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt   # tensorflow ~1GB+, lâu

# Tạo schema
alembic upgrade head
","h1"
"Section:backend/docs/upload_denoise_review.md:L251:Tạo schema","Tạo schema","backend/docs/upload_denoise_review.md",251,253,1,"
# Tạo schema
alembic upgrade head

# Chạy API
uvicorn app.main:app --reload
```","h1"
"Section:backend/docs/upload_denoise_review.md:L254:Chạy API","Chạy API","backend/docs/upload_denoise_review.md",254,286,1,"
# Chạy API
uvicorn app.main:app --reload
```

Mở **http://localhost:8000/docs**:

1. `POST /api/v1/auth/dev-login` (body mặc định OK) → copy `access_token`.
2. Click **Authorize**, dán token (không cần ""Bearer ""), Authorize → Close.
3. `POST /api/v1/documents` body `{""title"":""Test""}` → lấy `doc_id`.
4. `POST /api/v1/documents/{doc_id}/pages/upload` → chọn ảnh tài liệu (jpg/png) → response `status: denoised` + `denoised_url`.
5. `GET /api/v1/pages/{page_id}/download?type=denoised` → trình duyệt redirect sang Cloudinary tải file.

---

## 7. Hạn chế / việc còn lại

- **Auth thật chưa xong**: chỉ có `/auth/dev-login`. `register`, `login`, `refresh`, `logout` vẫn là 501. Khi triển khai login thật, có thể bỏ luôn dev-login.
- **Không có rate limit**: `slowapi` có trong requirements nhưng chưa wire.
- **Race condition `page_number`**: hai upload song song có thể tính cùng số → UniqueConstraint catch nhưng phải retry. Production nên dùng `SELECT ... FOR UPDATE` trên document hoặc sequence riêng.
- **Không có virus scan / image sanity check**: dev OK, prod nên thêm.
- **Không stream upload**: đang đọc full bytes vào memory → ảnh lớn (~MAX_UPLOAD_SIZE_MB=20) còn chấp nhận được, nhưng nếu tăng giới hạn nên stream qua disk.
- **Model load lần đầu chậm**: U-Net + load weights `.h5` lần đầu khoảng 5-15s. `@lru_cache(maxsize=1)` giữ model trong process, các request sau nhanh.
- **Worker Celery thật chưa có script chạy**: khi tắt `ALWAYS_EAGER`, cần `celery -A app.workers.celery_app worker --loglevel=info` (Windows phải dùng `--pool=solo` hoặc chạy qua WSL/Docker).
- **OCR + LLM correction**: file endpoint đã có nhưng đều 501 — bước tiếp theo theo thiết kế ""pipeline có điểm dừng"".

---

## 8. Tham chiếu nhanh

- Thiết kế tổng: [He_thong.md](../../He_thong.md)
- Model U-Net: [app/ai/denoising/](../app/ai/denoising/)
- Cấu hình: [app/core/config.py](../app/core/config.py), [.env.example](../.env.example)
","h1"
"Section:backend/docs/upload_denoise_review.md:L268:7. Hạn chế / việc còn lại","7. Hạn chế / việc còn lại","backend/docs/upload_denoise_review.md",268,280,2,"
## 7. Hạn chế / việc còn lại

- **Auth thật chưa xong**: chỉ có `/auth/dev-login`. `register`, `login`, `refresh`, `logout` vẫn là 501. Khi triển khai login thật, có thể bỏ luôn dev-login.
- **Không có rate limit**: `slowapi` có trong requirements nhưng chưa wire.
- **Race condition `page_number`**: hai upload song song có thể tính cùng số → UniqueConstraint catch nhưng phải retry. Production nên dùng `SELECT ... FOR UPDATE` trên document hoặc sequence riêng.
- **Không có virus scan / image sanity check**: dev OK, prod nên thêm.
- **Không stream upload**: đang đọc full bytes vào memory → ảnh lớn (~MAX_UPLOAD_SIZE_MB=20) còn chấp nhận được, nhưng nếu tăng giới hạn nên stream qua disk.
- **Model load lần đầu chậm**: U-Net + load weights `.h5` lần đầu khoảng 5-15s. `@lru_cache(maxsize=1)` giữ model trong process, các request sau nhanh.
- **Worker Celery thật chưa có script chạy**: khi tắt `ALWAYS_EAGER`, cần `celery -A app.workers.celery_app worker --loglevel=info` (Windows phải dùng `--pool=solo` hoặc chạy qua WSL/Docker).
- **OCR + LLM correction**: file endpoint đã có nhưng đều 501 — bước tiếp theo theo thiết kế ""pipeline có điểm dừng"".

---

## 8. Tham chiếu nhanh

- Thiết kế tổng: [He_thong.md](../../He_thong.md)","h2"
"Section:backend/docs/upload_denoise_review.md:L281:8. Tham chiếu nhanh","8. Tham chiếu nhanh","backend/docs/upload_denoise_review.md",281,286,2,"
## 8. Tham chiếu nhanh

- Thiết kế tổng: [He_thong.md](../../He_thong.md)
- Model U-Net: [app/ai/denoising/](../app/ai/denoising/)
- Cấu hình: [app/core/config.py](../app/core/config.py), [.env.example](../.env.example)
","h2"
