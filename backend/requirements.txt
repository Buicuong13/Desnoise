fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
celery==5.4.0
# On Windows, run the worker with Celery's built-in thread pool (no extra deps,
# no C compiler): celery -A app.workers.celery_app worker -P threads -c 4
redis==5.1.0
cloudinary==1.41.0
pillow==10.4.0
opencv-python==4.10.0.84
numpy==1.26.4
tensorflow==2.17.0
# Document/non-document upload classifier (MobileNetV3-Small). CPU wheels are
# the default on Windows/Linux. If you need an explicit CPU build, install with:
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
torchvision==0.19.1
pytesseract==0.3.13
# DocLayout-YOLO document-layout detector (DocStructBench checkpoint). Pulls in
# albumentations/scipy/etc. WARNING: those deps prefer numpy>=2, which is ABI-
# incompatible with tensorflow 2.17 (breaks denoise). ALWAYS install via this
# file (or pin `numpy==1.26.4` on the command line) so numpy stays on 1.26.4 —
# never `pip install doclayout-yolo` on its own.
doclayout-yolo==0.0.3
langchain==0.3.3
langchain-openai==0.2.2
langchain-core==0.3.10
python-docx==1.1.2
docx2pdf==0.1.8
reportlab==4.2.5
slowapi==0.1.9
httpx==0.27.2
python-dotenv==1.0.1
# Stripe Checkout for the billing/subscription flow (see services/billing_service.py).
stripe==11.1.0
